# 사용자가 직접 해야 할 일

클로드 코드가 대신 할 수 없는 것들만 모았다. 계정 가입, 키 발급, 결제, 사이트 설정처럼
**사람이 로그인해서 클릭해야 하는 일**이다. 코드 작성은 여기에 없다.

- 진행하다 필요한 시점이 오면 클로드 코드가 항목 하나를 골라 화면 단위로 안내한다.
- 미리 앞질러 할 필요 없다. 순서는 클로드 코드가 관리한다.
- 발급받은 키는 전부 `.env` 파일에만 넣는다. 채팅창에 붙여넣지 않는다.

최종 갱신: 2026-08-11

---

## 지금 (0~1단계)

- [x] **개발 도구 설치** — Python 3.12.10 / Node.js v24.18.0 / Git 2.55.0 모두 설치 확인 (2026-08-10)
- [ ] **토스증권 계좌 개설** — 토스 앱 → 주식 탭. 비대면, 신분증 필요. **10~20분**
      ※ 이미 계좌가 있으면 건너뛴다.
- [ ] **토스증권 Open API 키 발급** — PC 웹 WTS(tossinvest.com) 로그인 → 설정 > Open API →
      `client_id`·`client_secret` 발급. **5분**
      ※ 순차 개방 중이라 메뉴가 안 보일 수 있다. 그러면 사전신청만 하고 클로드 코드에게 알린다 —
      미국 기업 데이터(EDGAR) 쪽을 먼저 진행하도록 순서를 바꾼다.
- [ ] **토스증권 허용 IP 등록** — 같은 화면 하단 "허용 IP 관리"에 **`14.4.110.71`** 등록. **2분**
      ※ 2026-08-11 확인한 집 공인 IP다.
      ※ 등록 안 된 IP에서 부르면 무조건 403으로 막힌다.
      ※ 가정용 인터넷은 IP가 바뀔 수 있다. 잘 되던 게 갑자기 403이면 이것부터 의심한다.

## 곧 (국내 공시·재무를 붙일 때)

- [ ] **OpenDART 인증키 발급** — opendart.fss.or.kr → 오픈API 신청. 무료, 즉시 발급, 이메일 인증. **5분**
      ※ 국내 종목의 공시·재무 데이터 전부가 여기서 나온다. 이 프로젝트의 핵심 데이터 소스.

## 4단계 (미국 기업 데이터)

- [ ] **SEC EDGAR용 이름·이메일 알려주기** — 가입 절차 없음. 가입도 키도 필요 없다.
      다만 호출할 때 신원을 밝히는 헤더에 넣을 **이름과 이메일**이 필요하다. **1분**
      ※ 이 이메일은 SEC 서버에 전달된다. 평소 쓰는 주소면 된다.

## 6단계 (10-K 서술 분석)

- [ ] **Anthropic API 키 발급** — platform.claude.com → 가입 → Settings > API Keys. 신용카드 등록 필요. **10분**
- [ ] **사용량 상한·알림 설정** — 같은 콘솔의 Usage/Limits. **5분**
      ※ 코드 버그로 같은 문서를 반복 분석하는 사고가 개인 프로젝트 비용 사고의 대부분이다.
      키 발급 직후 반드시 같이 한다.

## 나중 (매크로 지표 — 선택)

- [ ] **한국은행 ECOS 인증키** — ecos.bok.or.kr → 오픈API. 무료. **5분**
- [ ] **FRED API 키** — fred.stlouisfed.org → My Account → API Keys. 무료. **5분**
      ※ 금리·CPI·유가 등 대시보드 상단 매크로 스트립용.
      ※ 이미 운영 중인 `시황/` 프로젝트와 지표가 겹친다. 그쪽 키를 재사용할 수 있는지 그때 확인한다.
- [ ] **공공데이터포털 / KRX Open API** — data.go.kr. 종목 마스터·과거 일별 데이터 보조용. **10분**
      ※ 실시간이 아니다. 없어도 진행에 지장 없다.

## 8단계 (외부 공개·배포)

- [ ] **GitHub 계정·저장소** — 포트폴리오 링크로 쓸 저장소. 이미 계정 있음(greyx9-sketch). **5분**
- [ ] **VPS 서버 계약** — Vultr / Linode 월 $5~10 또는 Oracle Cloud 무료 티어. **30분**
      ※ **고정 IP가 반드시 필요하다.** 토스 허용 IP에 등록해야 하기 때문. 계약 전 이 조건부터 확인.
- [ ] **VPS IP를 토스 허용 IP에 추가 등록** — 서버 IP가 나온 뒤. **2분**
- [ ] **도메인 구입** — 가비아 / Cloudflare 등. 연 1~2만원. **20분**
      ※ 없어도 IP로 접속은 되지만, 이력서에 적을 주소라면 있는 편이 낫다.
- [ ] **공용 비밀번호 정하기** — 사이트 접근용 비밀번호 하나. 이력서에 URL과 함께 적을 것. **1분**

---

## 진행 상황 메모

- 2026-08-10 — 환경 점검 완료. 설치할 것 없음. 토스증권 키 발급부터 시작.
- 2026-08-10 — 0단계 완료. 폴더 구조·Git 저장소·`.gitignore`·`.env` 생성,
  가상환경과 패키지 설치, `GET /health` → `{"status":"ok"}` 동작 확인.
- 2026-08-11 — 1단계 코드 작성 완료(`backend/app/clients/toss.py`, `backend/scripts/check_toss.py`).
  가짜 서버로 토큰 캐싱·401 재발급·rate limit·403 안내·출력 형식까지 검증했다.
  **실제 호출은 토스 키 발급 + 허용 IP(`14.4.110.71`) 등록을 기다리는 중.**
- 2026-08-10 — 기획 문서 3종이 `D:\증권정보` 로 옮겨져 있던 것을 이 폴더로 복사해 되돌림.
  프로젝트 폴더는 `D:\d클로드 1\stock information` 하나로 확정.
  `D:\증권정보` 는 남아 있으나 쓰지 않는다. 나중에 지워도 된다.
