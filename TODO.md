# 사용자가 직접 해야 할 일

클로드 코드가 대신 할 수 없는 것들만 모았다. 계정 가입, 키 발급, 결제, 사이트 설정처럼
**사람이 로그인해서 클릭해야 하는 일**이다. 코드 작성은 여기에 없다.

- 진행하다 필요한 시점이 오면 클로드 코드가 항목 하나를 골라 화면 단위로 안내한다.
- 미리 앞질러 할 필요 없다. 순서는 클로드 코드가 관리한다.
- 발급받은 키는 전부 `.env` 파일에만 넣는다. 채팅창에 붙여넣지 않는다.

최종 갱신: 2026-08-18 (**수급 동향까지 배포 완료** — 사용자가 할 일은 국내 장중 확인뿐)

---

## 📍 다음에 여기서 이어서 한다

**지금 상태**: 대시보드가 인터넷에 떠 있고 국내·미국 화면이 정상 동작한다.
**http://129.225.188.89** · 아이디·비밀번호는 `.env` 의 `SITE_USER`·`SITE_PASSWORD`

기능은 8단계까지 전부 끝났다. 아래는 **하면 좋은 것**이지 안 하면 망가지는 것이 아니다.
급한 순서대로 적었다.

| 순위 | 할 일 | 누가 | 시간 | 왜 |
| --- | --- | --- | --- | --- |
| ~~—~~ | ~~가동 감시 붙이기~~ | — | — | ✅ **완료 (2026-08-16)** — 아래 참고 |
| ~~—~~ | ~~10-K 서술 분석 (6단계)~~ | — | — | ✅ **완료·배포됨 (2026-08-17)** — 아래 참고 |
| ~~—~~ | ~~비밀번호 늘리기~~ | — | — | 안 하기로 함 (2026-08-17) |
| ~~—~~ | ~~국내(DART) 사업보고서 분석~~ | — | — | ✅ **완료·배포됨 (2026-08-17)** — 아래 참고 |
| ~~—~~ | ~~도메인 + HTTPS~~ | — | — | 사지 않기로 함 (2026-08-17) |
| ~~—~~ | ~~매크로 지표 스트립~~ | — | — | ✅ **완료·배포됨 (2026-08-17)** — 아래 참고 |
| **1** | **국내 개장 중 실시간 갱신 확인** | 클로드 | 5분 | 미국은 확인됐다(아래). 국내는 8/18 09:00 |

각 항목의 자세한 내용은 아래 해당 절에 있다.

### 개발 backlog (클로드가 혼자 하는 것 — 사용자 준비 불필요)

위 표는 **사용자가 할 일**만 담는다. 그것만 보면 "남은 게 없다"로 읽히는데 아니다.
새 키도 결정도 필요 없이 클로드가 바로 할 수 있는 것들을 여기 따로 적는다.

| 순위 | 할 일 | 시간 | 왜 |
| --- | --- | --- | --- |
| ~~—~~ | ~~README~~ | — | ✅ 완료 (2026-08-17). 저장소 첫 화면 + 스크린샷 |
| ~~—~~ | ~~테스트~~ | — | ✅ 완료 (2026-08-18). pytest 55개, 실제 겪은 버그를 회귀로 |
| ~~—~~ | ~~DB 백업~~ | — | ✅ 완료 (2026-08-18). 매일 06:20 KST cron |
| ~~—~~ | ~~수급 동향~~ | — | ✅ **완료·배포됨 (2026-08-18)** — 아래 참고 |
| **1** | **관심종목(watchlist)** | 2시간 | 기획서 5.1 의 관심종목 그리드. 실사용에 유용 |
| 2 | 앱 오류 알림 | 1시간 | 오라클 경보는 VM 정지만 잡는다. 앱이 500 을 뿜어도 알 방법이 없다 |
| 3 | 라우터·서비스 통합 테스트 | 2시간 | 지금 테스트는 순수 함수만 덮는다 |
| 4 | 종목 메모(notes) · 일정(events) | 반나절 | 기획서 DB 설계에 있으나 미구현 |
| 5 | 국내 분기·반기 재무 | 반나절 | 누적/당분기 구분이 섞여 별도 처리가 필요하다 |

### ✅ 수급 동향 — 배포 완료 (2026-08-18)

국내 종목 상세에서 시세표 바로 아래에 **수급** 블록이 생겼다. 새 키는 필요 없었다 —
토스 API 가 종목별로 다섯 자료를 다 준다.

- **투자자별 순매수** 5거래일 표 (개인·외국인·기관, 단위는 주)
- **공매도 비중** · **신용융자 잔고율** · **대차 잔고** · **프로그램 순매수**

다섯 표를 그대로 늘어놓지 않고 위처럼 압축했다. 종목 상세는 이미 길어서 원자료를
나열하면 읽히지 않는다.

**지표마다 기준일을 따로 적었다.** 공매도·대차거래는 당일 18~19시에, 신용거래·투자자별은
다음 영업일 04시에 갱신된다. 하나의 날짜로 뭉뚱그리면 없는 자료를 있는 것처럼
보이게 된다. 한 자료를 못 받아도 나머지는 그대로 보여준다.

※ **국내 전용이다.** 토스가 미국 종목에는 이 자료를 주지 않는다.

### ✅ 10-K 분석 — 배포 완료 (2026-08-17)

사이트에서 미국 종목을 열면 재무표 아래에 **"10-K 분석하기"** 버튼이 있다. 누르면 최신
10-K 를 읽고 30초~2분 뒤에 사업·위험요인·경영진 설명이 한국어로 뜬다. 한 번 분석한
종목은 저장돼 다음부터는 즉시 뜨고 돈이 들지 않는다.

- 서버 `.env` 에 `ANTHROPIC_API_KEY` 와 `ANALYSIS_DAILY_LIMIT=20` 을 넣었다.
- 서버에서 AAPL 분석을 실제로 돌려 확인했다(36초, 실질 위험 5건, 약 190원).
- 사용량과 누적 비용은 `http://129.225.188.89/api/us/analysis/usage` 에서 볼 수 있다.

**하루 상한 20건**이다. 바꾸려면 서버 `.env` 의 `ANALYSIS_DAILY_LIMIT` 를 고치고
서비스를 재시작한다(`sudo systemctl restart stock-dashboard`).

### ✅ 미국 장중 갱신 — 확인됨 (2026-08-17)

미국 데이마켓 세션에서 화면 배지가 `데이마켓 · 5초마다 갱신` 으로 바뀌고 체결 시각이
실제로 갱신되는 것을 확인했다(14:30 → 14:38). 폴러는 정상이다.
국내는 오늘이 광복절 대체공휴일이라 휴장이었다 — **8/18(화) 09:00** 에 확인한다.

### ✅ 가동 감시 — 완료 (2026-08-16)

오라클 무료 인스턴스는 **7일간 CPU·네트워크 사용률이 15% 미만이면 정지된다.**
실제로 이 서버의 CPU 는 0~3% 대라 회수 대상 프로필이 맞다(콘솔 지표로 확인).
종량제로 바꾸면 면제되는데 사업자등록번호를 요구해서 못 했다(아래 8단계 절 참고).

**UptimeRobot 대신 오라클 자체 경보로 붙였다** — 새 가입이 필요 없고 콘솔 안에서 끝난다.

| 구성 요소 | 값 |
| --- | --- |
| 알림 주제 | `stock-alerts` (Notifications) |
| 구독 | `greyx9@gmail.com` · Email · **Active** (확인 메일 클릭 완료) |
| 경보 | `stock-server-down` · Critical · Active |
| 감시 지표 | `oci_compute_instance_health` / `InstanceAccessibilityStatus` |
| 조건 | **absent (지표 끊김) 5분** → 메일 발송 |

- 5분 넘게 지켜봐서 오작동(항상 울림)이 없는 것을 확인했다.
- 테스트 메시지를 발행해 메일 수신 경로도 확인했다.
- 메일이 오면: 오라클 콘솔 → 컴퓨트 → 인스턴스 → **Start**. systemd 가 대시보드를 자동으로 띄운다.

**이 방식의 한계** — 가상머신이 정지된 것은 잡지만, **서버는 켜져 있는데 앱이나 Caddy 만 죽은
경우는 못 잡는다.** 다만 systemd 에 `Restart=always` 가 걸려 있어 앱이 죽으면 5초 뒤 자동으로
다시 뜬다. 그래도 확실히 하려면 나중에 UptimeRobot 으로 URL 자체를 감시하면 된다
(그때는 401 을 정상으로 취급하도록 설정해야 한다 — 비밀번호가 걸려 있어서다).

### 1. 국내 개장 중 확인 (8/18 화 09:00)

미국은 위에서 확인됐다. 국내만 남았다 — 8/17 이 광복절 대체공휴일이라 휴장이었다.

- 화면 상단 배지가 `정규장 · 5초마다 갱신` 으로 바뀌는지
- 현재가가 실제로 갱신되는지
- `sudo journalctl -u stock-dashboard -f` 에 403 이나 429 가 없는지

※ 휴장 중에도 토스가 마지막 체결가를 내려주는 것은 이미 확인했다. 시세 경로 자체는
살아 있고, 남은 건 국내 장중 폴링 동작뿐이다.

### ✅ 국내(DART) 사업보고서 분석 — 배포 완료 (2026-08-17)

국내 종목 상세에서 재무표 아래 **"사업보고서 분석하기"** 버튼을 누르면, 최신 사업보고서의
사업 내용·위험·경영진단이 1~3분 뒤에 뜬다. 미국 10-K 분석과 같은 구조·같은 상한이다.

**국내 서식의 특징을 그대로 반영했다.** 10-K 에는 「위험요인」 항목이 따로 있지만 국내
사업보고서에는 없다. 위험이 「위험관리 및 파생거래」와 「투자자 보호를 위하여 필요한 사항」
(소송·제재·우발부채)에 흩어져 있어서, 그걸 모아 주고 **각 위험이 어디서 나왔는지 출처를
함께 표시**한다. 삼성전자에서 중대재해·해외 종속기업 제재·미국 반도체 보조금 지급보증
같은 것들이 실제로 잡혔다.

- 실측 비용: 보고서 1건에 약 320~340원. 미국(190~290원)보다 조금 비싸다.
- **하루 상한 20건은 국내·미국 합산이다.** 지갑이 하나이므로 상한도 하나다.
- 사용량: `http://129.225.188.89/api/us/analysis/usage` (국내·미국 합산)

### 도메인 + HTTPS — 사지 않기로 함 (2026-08-17)

지금 주소는 `http://129.225.188.89` 이고 HTTPS 가 아니다. 나중에 마음이 바뀌어 도메인을
사면(연 1~2만원), A 레코드를 이 IP 로 걸고 `deploy/Caddyfile` 의 주석 처리된 HTTPS
블록으로 바꾸는 것만으로 Caddy 가 인증서를 알아서 받는다. 아이디·비밀번호는 그대로 쓴다.

### ✅ 아이디·비밀번호 — 이대로 두기로 함 (2026-08-17)

둘 다 `.env` 의 `SITE_USER`·`SITE_PASSWORD` 한 곳에서 관리한다. 고친 뒤 서버에서
`bash /opt/stock/deploy/setup_caddy.sh` 를 돌리면 반영된다 — Caddyfile 은 손대지 않는다.

비밀번호가 4자지만 늘리지 않기로 했다. bcrypt cost 14 라 크랙은 사실상 불가능하고,
읽기 전용 대시보드라 위험이 제한적이다.

### ✅ 매크로 지표 스트립 — 배포 완료 (2026-08-17)

화면 맨 위에 지표 띠가 붙었다. 국내·미국 탭 어디서나 보인다. 항목에 마우스를 올리면
출처와 기준일이 나온다.

**코스피 · 코스닥 · S&P500 · 원/달러 · 한국 기준금리 · 미국 기준금리 · WTI 유가**

| 지표 | 출처 | 갱신 |
| --- | --- | --- |
| 코스피 · 코스닥 · 원/달러 | 토스증권 | 장중 실시간 |
| S&P500 | 토스증권 (SPY) | 미국장 중 실시간 |
| 한국·미국 기준금리 | **`시황` 프로젝트 공개 결과물** | 하루 1회 |
| WTI 유가 | FRED | 하루 1회 |

**새 키는 FRED 하나만 늘었다.** 기준금리는 이미 운영 중인 `시황` 프로젝트가 수집한
결과물(`greyx9-sketch.github.io/macro/data/dashboard.json`)을 읽는다 — 기획서에 적힌
대로 같은 수집을 두 번 하지 않는 것이고, 무엇보다 두 화면의 숫자가 항상 일치한다.
FRED 키는 `시황` 에서 쓰던 것과 같은 키를 쓴다(계정 단위라 재사용이 정상).

밝혀 둘 것 둘:
- **S&P500 은 지수가 아니라 추종 ETF(SPY) 가격이다.** 토스에 미국 지수가 없다.
  등락률은 사실상 같지만 값이 지수가 아니므로 툴팁에 그대로 적어 두었다.
- **국고채 3년은 뺐다.** 토스 지표 심볼을 20가지 시도했으나 전부 거절이고 카탈로그가
  공개돼 있지 않다. 매일 보는 값으로는 정책금리가 낫다고 판단했다.

한 곳이 죽어도 나머지는 보여준다. 마지막 성공값을 DB(`macro_values`)에 남겨 두고,
못 받으면 "갱신지연" 표시와 함께 그것을 보여준다.

---

## 지금 (0~1단계)

- [x] **개발 도구 설치** — Python 3.12.10 / Node.js v24.18.0 / Git 2.55.0 모두 설치 확인 (2026-08-10)
- [x] **토스증권 계좌 개설** — 완료 (2026-08-11)
- [x] **토스증권 Open API 키 발급** — `client_id`·`client_secret` 발급받아 `.env`에 입력 완료 (2026-08-11)
- [x] **토스증권 허용 IP 등록** — `14.4.110.71` 등록 완료 (2026-08-11)
      ※ 2026-08-11 확인한 집 공인 IP다.
      ※ **가정용 인터넷은 IP가 바뀔 수 있다.** 잘 되던 게 갑자기 403이면 이것부터 의심한다.
      현재 IP는 whatismyip.com 에서 확인하거나 클로드 코드에게 물어보면 된다.

## 지금 (2단계 — KRX 공식 종가)

- [x] **공공데이터포털 가입 + 주식시세정보 API 활용신청** — 인증키 발급받아 `.env` 입력 완료 (2026-08-11)
      ※ 신청한 API: **금융위원회_주식시세정보** (`getStockPriceInfo`)
      ※ 제약: 공식 종가는 **다음 영업일 오후 1시 이후**에 올라온다. 장중 실시간용이 아니라
      확정 종가 아카이브용이다. 장중 등락률은 토스 랭킹 기준가로 채운다.
      ※ 개발계정 일일 호출 한도 10,000건. 종가는 하루 한 번만 바뀌므로 DB 에 저장해 재사용한다.

## 완료 (국내 공시)

- [x] **OpenDART 인증키 발급** — 발급받아 `.env` 에 입력 완료 (2026-08-11)
      ※ 국내 종목의 공시·재무 데이터 전부가 여기서 나온다. 이 프로젝트의 핵심 데이터 소스.
      ※ 일일 호출 한도 20,000건.

## 완료 (미국 기업 데이터)

- [x] **SEC EDGAR용 이름·이메일** — `.env` 의 `SEC_USER_AGENT` 에 설정 완료 (2026-08-11)
      ※ 이 값은 호출할 때마다 SEC 서버에 전달된다. 바꾸고 싶으면 `.env` 의
      `SEC_USER_AGENT=` 뒤를 "이름 이메일" 형식으로 고치면 된다.
      ※ 가입도 키도 없다. 신원 표기만 있으면 된다. 없으면 403.

## 6단계 (10-K 서술 분석) ✅ 완료·배포됨 (2026-08-17)

미국 종목의 10-K 에서 사업 내용·위험요인·MD&A 를 LLM 이 읽고 서술형으로 정리해 준다.
**숫자는 LLM 에게 맡기지 않는다**(절대 규칙 3) — 응답 스키마에 수치 필드를 아예 두지 않아
끼워 넣을 자리가 없다. 매출·이익은 지금처럼 XBRL 에서 계산해 재무표로 보여준다.

- [x] **Anthropic API 키 발급** — 완료 (2026-08-17). `.env` 의 `ANTHROPIC_API_KEY`
- [x] **사용량 상한 설정** — 완료. 콘솔에서 월 상한.
- [x] **모델 선택** — **Claude Sonnet 5**. 기획서의 "기본 Haiku" 를 이 기능에 한해 올렸다.
      위험요인·MD&A 는 법무적으로 쓰인 긴 문장이라 요약이 아니라 해석이 필요하다.
- [x] **섹션 추출기 · 저장 테이블 · 분석 서비스 · API · 화면** — 전부 완료

**실측 비용**: 문서 1건에 약 190~290원(정가 기준 추정). 한 번 분석한 문서는 DB 에
저장하고 다시 부르지 않으므로 같은 종목을 다시 열면 0원이다.

**비용 상한 다섯 겹** — 콘솔 월 상한(사용자), DB 캐시, 하루 20건 상한,
동시 1건, 호출 전 토큰 상한 확인. 사용량은 `/api/us/analysis/usage` 에서 볼 수 있다.

※ Batch API 는 쓰지 않았다. 50% 싸지만 결과가 최대 24시간 뒤에 나와서 "버튼 누르면
분석" 방식과 맞지 않는다. 나중에 상위 종목을 밤사이 미리 채워 두기로 하면 그때 붙인다.

## 매크로 지표 ✅ 완료·배포됨 (2026-08-17)

- [x] **FRED API 키** — 완료. `시황` 프로젝트에서 쓰던 키를 그대로 재사용했다.
      FRED 키는 계정 단위라 여러 프로젝트에서 같이 쓰는 것이 정상이다. 호출량도
      문제없다(허용 분당 120회 / 이 프로젝트는 6시간에 1회).
      ⚠️ 나중에 이 키를 폐기·교체하면 **`시황` 과 이 프로젝트가 같이 멈춘다.**
- [~] **한국은행 ECOS 인증키** — **필요 없어졌다.** 기준금리를 `시황` 프로젝트의 공개
      결과물에서 읽으므로 ECOS 를 직접 부르지 않는다. 국고채 3년을 나중에 꼭 넣고 싶어지면
      그때 발급받으면 된다(ecos.bok.or.kr → 오픈API, 무료 5분).

상세는 위 "매크로 지표 스트립" 절 참고.

## 8단계 (외부 공개·배포) ✅ 완료 (2026-08-15)

**결정**: Oracle Cloud 무료 티어 + 도메인 없이 IP 접속. **홈 리전 오사카** (2026-08-14)

한국 리전은 2026-08-11·08-14 두 번 모두 가입 화면 목록에 나오지 않아 포기했다.
**오사카여도 성능 문제는 없다** — 서울↔오사카 왕복 30~40ms 라 5초 폴링·화면 로딩에서
체감되지 않는다. 리전은 속도가 아니라 용량 확보 관점에서 고르면 된다.

**막히면 갈아탈 곳**: AWS Lightsail 서울 — $7/월, **첫 3개월 무료**, 1GB/40GB, 고정 IP 포함,
방화벽도 한 군데만 열면 된다. `deploy/` 의 설정 파일들은 그대로 쓰고
`배포방법.md` 의 1~5단계만 갈아끼우면 된다.

상세 절차는 `deploy/배포방법.md` 에 12단계로 적어 두었다. 아래는 체크리스트다.

- [x] **GitHub 저장소 만들고 코드 올리기** — 완료 (2026-08-11)
      https://github.com/greyx9-sketch/stock-dashboard (공개)
      ※ 코드 수정 후에는 `git push` 로 올리고, 서버에서 `deploy/update.sh` 를 돌린다.
- [x] **Oracle Cloud 가입** — 완료 (2026-08-14). 홈 리전 **오사카**. 나중에 못 바꾼다.
- [~] **종량제(Pay As You Go) 전환** — ❌ **불가로 판정 (2026-08-15)**
      카드 등록까지는 됐으나, 업그레이드 화면이 `Individual` 계정에도 **세금 등록번호
      (사업자등록번호)를 필수**로 요구한다. 개인은 채울 수 없어 포기했다.
      ※ 그래서 **유휴 회수 위험이 남아 있다.** 오라클은 7일간 CPU·네트워크 사용률이
      15% 미만인 무료 인스턴스를 **정지**시킨다(삭제 아님 — 데이터·설정은 남는다).
      복구는 콘솔에서 **Start** 한 번. systemd 가 대시보드를 자동으로 다시 띄운다.
      → 아래 "가동 감시" 항목으로 대비한다.
- [x] **가동 감시 붙이기** — 완료 (2026-08-16). 오라클 자체 경보로 붙였다.
      `stock-alerts` 주제 + greyx9@gmail.com 구독(Active) + `stock-server-down` 경보
      (지표 5분 끊기면 메일). 상세는 맨 위 "가동 감시 — 완료" 절 참고.
- [x] **예산 알림 걸기** — `stock` 예산, 월 1 USD, 100% 초과 시 greyx9@gmail.com 으로 메일
      ※ 종량제로 안 갔으므로 청구 위험은 사실상 없지만, 안전장치로 그대로 둔다.
- [x] **서버(인스턴스) 생성** — 완료 (2026-08-15). Ubuntu 24.04 / **VM.Standard.E2.1.Micro**
      (1 OCPU · 1GB · 45GB). Ampere A1 은 오사카 형상 목록에 없어서 E2 로 갔다.
      ※ 이미지 선택 함정 둘: 기본값이 **Oracle Linux 9** 라 반드시 Ubuntu 로 바꿔야 하고,
      Ubuntu 24.04 중 **`aarch64` 붙은 빌드는 E2(x86)에서 거부**된다(`Shape ... is not valid for image`).
      ※ SSH 키: `C:\Users\greyx\.ssh\oracle-stock.key` (원본은 Downloads 에도 있음)
      ※ 접속: `ssh -i ~/.ssh/oracle-stock.key ubuntu@129.225.188.89`
- [x] **공용 IP를 "예약된 IP"로 바꾸기** — 완료. **129.225.188.89** (`stock-ip`)
      ※ 콘솔 함정: VNIC 의 ⋮ 메뉴에 있는 "Reserve IPv4 address" 는 **사설 IP** 용이라 다른 것이다.
      순서는 ① IP management → Reserved public IPs 에서 먼저 생성 → ② VNIC → IP administration
      → ⋮ → Edit 에서 "No public IP" 로 바꿔 임시 IP 를 떼고 → ③ 다시 Edit 하면 그때
      "Reserved public IP" 선택지가 나타난다.
- [x] **방화벽 80번 포트 열기** — 완료. 클라우드(Default Security List, 0.0.0.0/0 TCP 80)와
      서버 iptables(INPUT 5번 규칙, `netfilter-persistent save`) 양쪽 다.
- [x] **스왑 2GB** — 완료. 메모리가 1GB 뿐이라 화면 빌드에 필요하다.
- [x] **프로그램 설치·코드 배포** — 완료. Python 3.12.3 / Node 22.23.2 / Caddy 2.11.4,
      `/opt/stock` 에 클론, venv·프론트 빌드 성공, systemd 등록 후 `/health` → `{"status":"ok"}`.
      로컬 `data/app.db` 를 그대로 올려 KRX 232,953행·DART 3,981건·SEC 10,387건을 재수집 없이 이어받았다.
- [x] **접근 아이디·비밀번호 정하기** — 완료 (2026-08-15, 2026-08-17 개선).
      둘 다 `.env` 의 `SITE_USER`·`SITE_PASSWORD` 에 있다. 서버에서 해시로 바꿔
      `/etc/caddy/Caddyfile` 에 넣는다 — Caddyfile 은 자리표시자만 들고 있고 손대지 않는다.
      ※ 안내서에 빠져 있던 함정 하나를 이번에 찾아 보완했다 — `/var/log/caddy` 폴더를 미리
      만들고 caddy 소유로 바꾸지 않으면 **Caddy 가 기동 자체를 못 한다**(setup_caddy.sh 가 처리).
      ※ 비밀번호가 4자다. 위 "2. 아이디·비밀번호" 절 참고.
- [x] **서버 IP를 토스 허용 IP에 등록** — 완료 (2026-08-15). **129.225.188.89**
      등록 전에는 `IP address not allowed (code=access_denied)` 가 났고, 등록 후 사라졌다.
- [x] **재부팅 내구성 확인** — `sudo systemctl reboot` 후 **30초 만에 자동 복구**.
      stock-dashboard·caddy 둘 다 enabled, 스왑 2GB·iptables 80 규칙·예약 IP 모두 유지됨.
- [x] **배포된 화면 눈으로 확인** — 브라우저로 국내·미국 두 화면 다 확인. **콘솔 오류 0건.**
      국내: 2,872종목 목록, 삼성전자 현재가 274,500원·확정 종가 268,000원·84거래일 차트·
      연간재무 6개년·공시 필터 전부 정상. 미국: 토스 거래대금 상위 + SEC 재무/공시 정상.
      ※ 인증(401/200)은 curl 로, 화면 렌더링은 SSH 터널(`-L 8899:127.0.0.1:8000`)로 확인했다.
      브라우저 기본 인증 창은 자동화 도구를 멈추게 해서 터널로 우회한 것이다.

---

## 운영 요령 (배포 후 자주 쓰는 것)

```bash
# 서버 접속
ssh -i ~/.ssh/oracle-stock.key ubuntu@129.225.188.89

# 코드를 고쳤을 때 — 집 PC 에서 git push 한 뒤 서버에서 (ubuntu 계정 그대로)
cd /opt/stock && ./deploy/update.sh

# 상태 확인
systemctl is-active stock-dashboard caddy
curl -s http://127.0.0.1:8000/health

# 로그 실시간
sudo journalctl -u stock-dashboard -f

# 아이디·비밀번호 바꾼 뒤 (.env 의 SITE_USER / SITE_PASSWORD 수정 후)
bash /opt/stock/deploy/setup_caddy.sh

# DB 백업 — 매일 06:20 KST 에 cron 이 자동으로 돈다. 손으로 볼 때:
sudo -u stock -H bash -c 'cd /opt/stock && ./.venv/bin/python backend/scripts/backup_db.py --list'
```

| 증상 | 먼저 볼 것 |
| --- | --- |
| 링크가 아예 안 열림 | 오라클 콘솔에서 인스턴스가 **정지**됐는지 (유휴 회수). Start 누르면 복구. 정지되면 `[stock] 서버 응답 없음` 메일이 온다 |
| 화면은 뜨는데 현재가가 빔 | 토스 허용 IP. 서버 IP 는 고정이라 안 바뀌지만 **집 IP 는 바뀔 수 있다** |
| 확정 종가가 며칠째 그대로 | 정상일 수 있다 — 확정 종가는 다음 영업일 오후 1시 이후에 올라온다 |
| 느려짐 | 메모리 1GB 다. `free -m` 으로 스왑을 얼마나 쓰는지 본다 |

※ 도메인은 사지 않기로 했다. 그래서 **HTTPS가 아니다** — 접근 비밀번호가 암호화되지 않고 오간다.
읽기 전용 대시보드라 피해는 제한적이지만, 나중에 도메인을 사면 `deploy/Caddyfile` 의 주석 블록으로
바꾸는 것만으로 HTTPS가 켜진다.

---

## 진행 상황 메모

- 2026-08-10 — 환경 점검 완료. 설치할 것 없음. 토스증권 키 발급부터 시작.
- 2026-08-10 — 0단계 완료. 폴더 구조·Git 저장소·`.gitignore`·`.env` 생성,
  가상환경과 패키지 설치, `GET /health` → `{"status":"ok"}` 동작 확인.
- 2026-08-11 — 등락률 기준가 조사. 토스 시세 API 에는 기준가 필드가 없고, 일봉 종가는
  시간외(20:00) 포함 마지막 체결가라 앱 화면 기준가와 다르다(삼성전자 8/7: 일봉 235,000 vs
  기준가 231,000). 16:00 봉·15:30 봉·상하한가 중간값으로 역산을 시도했으나 상위 30 종목
  검증에서 전부 실패 — 캔들에 KRX 와 NXT 체결이 섞인 것으로 보인다.
  현재 대응: 랭킹 API 가 주는 토스 공식 기준가를 우선 사용(거래대금 상위 100 종목),
  나머지는 일봉 기준으로 계산하고 화면에 기준을 명시. 확정 종가는 KRX 공공데이터로 보완 예정.
- 2026-08-11 — **1단계 완료.** 토스증권 키 발급·허용 IP 등록 후 실제 호출 성공.
  삼성전자(005930) 현재가 231,500원 / 전일대비 -3,500(-1.49%) 조회 확인.
  `/health` 도 그대로 동작(회귀 없음). 다음은 2단계 — 시세 폴러와 DB 저장.
- 2026-08-11 — **KRX 확정 종가 연결 확인.** 공공데이터포털 `getStockPriceInfo` 호출 성공.
  삼성전자 2026-08-07 확정 종가 **231,000원**이 **토스 공식 기준가 231,000원과 정확히 일치**했다
  (토스 일봉 종가는 235,000). 등락률 기준가 문제의 해법이 확인된 것이다.
  `backend/scripts/check_krx.py` 로 언제든 다시 대조할 수 있다.
- 2026-08-11 — **KRX 확정 종가 DB 적재 동작 확인.** 날짜 하나로 전 종목(2,872개)을 받을 수
  있어서 종목별이 아니라 날짜별로 적재한다. 하루치 = 3 호출(일일 한도 10,000).
  6 거래일 17,232 행 저장 완료. 재실행해도 중복이 생기지 않는다(upsert).
  `python backend/scripts/ingest_krx.py` 로 언제든 최근 날짜를 채울 수 있다.
  DB 파일은 `data/app.db` (Git 에 올라가지 않음).
- 2026-08-11 — **첫 화면 완성.** 종목 목록·검색·상세·차트가 브라우저에서 동작한다.
  현재가(토스)와 확정 종가(KRX)를 나란히 보여주고, 현재가의 기준가로 KRX 확정 종가를 쓴다.
  과거 데이터 80 거래일(230,082 행) 적재 완료.
  띄우는 방법은 프로젝트 루트의 `실행방법.md` 에 단계별로 적어 두었다.
- 2026-08-11 — **현재가 자동 갱신 완성.** 서버가 토스 현재가를 주기적으로 받아 두고
  화면이 그것을 읽는다. 토스 장 운영시간 API 로 세션을 판단해 정규장 5초 / 시간외 15초 /
  마감·휴장에는 호출하지 않는다(공휴일표 관리 불필요). 등락률 기준가는 KRX 확정 종가다.
  토스를 부르는 곳은 폴러 한 군데뿐이다 — 화면·상세 조회는 캐시만 읽어 항상 즉시 응답한다.
- 2026-08-11 — **확정 종가 자동 수집 완성.** 평일 13:20 에 자동 실행되고, 백엔드를 켤 때마다
  꺼져 있던 동안 빠진 날짜를 따라잡는다. 이제 매일 명령어를 칠 필요가 없다.
  화면 맨 아래에 수집 상태가 표시된다. 검증: 07-31 을 일부러 지운 뒤 작업을 돌려 2,872 행
  전부 복구되는 것을 확인했다.
- 2026-08-11 — **OpenDART 공시 연결.** 종목 상세에서 공시 목록을 보고 클릭하면 DART 원문으로
  간다. 유형 필터(정기·주요사항·거래소·지분·전체)가 핵심 — 없으면 대기업일수록 임원
  소유상황보고서로 도배된다(삼성전자 3,400건 중 3,328건이 지분공시). 기본값은 정기공시.
  DART 는 종목코드로 조회가 안 되고 고유번호(corp_code)가 필요해서, 상장사 3,981건 매핑을
  DB 에 넣고 주 1회 갱신한다.
- 2026-08-11 — **재무제표 연결.** 종목 상세에 최근 6개 연도 매출·영업이익·순이익 추이와
  영업이익률·ROE·부채비율이 나온다. 모든 수치는 XBRL 계정에서 직접 계산한다(절대 규칙 3).
  응답 하나가 3개 연도를 담고 있어 6년치가 2회 호출이면 끝난다.
  ※ 사업보고서(연간)만 다룬다. 분기·반기는 누적/당분기 구분이 섞여 있어 별도 처리가 필요하다.
- 2026-08-11 — **장중 동작 확인.** 오전 11시 45분경 화면에서 "정규장 · 5초마다 갱신" 배지와
  함께 현재가가 실시간으로 갱신되는 것을 확인했다. 폴러가 실제 장중에 정상 동작한다.
- 2026-08-11 — **SEC EDGAR 연결(백엔드).** 미국 기업 개요·공시·연간 재무를 조회할 수 있다.
  티커→CIK 매핑 10,387건 저장. AAPL·MSFT·JPM·WMT·TSLA 로 검증했다.
  ※ 회계연도 표기 주의: 이 프로젝트는 회계연도가 대부분 걸쳐 있는 달력 연도로 이름을 붙인다.
  월마트는 2025년 1월 말 결산을 스스로 'FY2025' 라 부르지만 여기서는 2024 로 표시된다.
  그래서 종료일(period_end)을 항상 함께 보여준다.
  ※ 아직 화면은 국내 전용이다. 미국 화면은 다음 단계.
- 2026-08-11 — **배포 준비 완료.** 백엔드 하나가 화면까지 내보내도록 바꿔 서버에서 프로세스를
  하나만 띄우면 되게 했다. Caddy 설정·systemd 서비스·갱신 스크립트·12단계 안내서를
  `deploy/` 에 만들었다. 로컬에서 배포 형태(8000 포트 단독)로 동작 확인.
  ※ uvicorn 은 **반드시 워커 1개**여야 한다. 토스 토큰이 client 당 1개만 유효해서
  프로세스가 여럿이면 서로의 토큰을 무효화한다. 폴러·스케줄러도 하나만 돌아야 한다.
- 2026-08-11 — **GitHub 공개 저장소에 코드 업로드 완료.**
  https://github.com/greyx9-sketch/stock-dashboard
  올리기 전 전체 커밋 이력을 검사해 `.env`·토스 키·DART 키·공공데이터포털 키가
  한 번도 들어간 적 없음을 확인했다. 업로드 후 GitHub 에서도 `.env` 404 확인.
  ※ 커밋 작성자 이메일은 공개된다(사용자 결정). 바꾸려면 이력 재작성이 필요하다.
- 2026-08-11 — **배포는 Oracle 가입 문제로 중단.** 다음에 이어서 한다.
- 2026-08-15 — **배포 완료. 사이트가 인터넷에 열렸다.** http://129.225.188.89
  한국 리전이 두 번 다 목록에 없어서 **오사카**로 갔다. 실측 왕복 31ms — 지연은 문제가 아니었다.
  콘솔에서 걸린 곳이 셋 있었다: ① 이미지 기본값이 Oracle Linux 9 ② Ubuntu 24.04 의
  `aarch64` 빌드는 E2(x86) 형상에서 거부됨 ③ 예약 IP 는 임시 IP 를 먼저 떼야 선택지가 나타남.
  서버에서는 `/var/log/caddy` 권한 때문에 Caddy 가 안 떠서 안내서에 10-1 단계로 추가했다.
  로컬 `data/app.db` 를 통째로 올려 재수집 없이 이어받았고, 서버의 KRX 수집기가 자동으로
  따라잡아 84거래일 241,566행이 됐다.
- 2026-08-17 — **6단계 10-K 서술 분석 완성(로컬).** 미국 종목 상세에서 버튼을 누르면
  최신 10-K 의 Item 1·1A·7 을 Sonnet 5 가 읽고 한국어로 정리한다. 위험요인은 실질/형식적으로
  갈라서 보여준다 — 10-K 위험요인의 상당수가 모든 회사에 똑같이 붙는 정형 문구라, 그걸
  걸러내는 것이 이 기능의 값어치다.
  만들면서 걸린 곳 넷: ① 본문 속 상호참조("…Item 1A 참조")가 진짜 제목보다 먼저 잡혔다
  ② 표를 통째로 버렸더니 제목을 표로 배치하는 월마트에서 항목을 하나도 못 찾았다
  ③ JPM 은 MD&A 를 "46–160쪽 참조"로만 적고 본문은 같은 파일 다른 자리에 둔다
  ④ effort 를 medium 으로 뒀더니 MSFT 에서 다섯 필드 중 하나만 채우고 끝냈다(high 로 해결).
  ①~③ 은 실제 10-K 10건을 돌려 보고서야 드러났다. 문서 형식이 회사마다 이렇게 다르다.
- 2026-08-17 — **매크로 스트립 완성·배포.** 화면 맨 위에 지표 7개 띠가 붙었다.
  조사해 보니 기획서 원안(ECOS+FRED 직접 호출)보다 나은 길이 있었다 — 지수·환율은
  토스에 이미 있고(키 있음, 실시간), 기준금리는 **이미 운영 중인 `시황` 프로젝트의
  공개 결과물**을 읽으면 된다. 그래서 새 키는 FRED 하나만 늘었다.
  같은 수집을 두 번 하지 않는 것이 기획서 의도이기도 했고, 무엇보다 두 화면의 숫자가
  어긋날 일이 없어진다.
  걸린 곳 둘: ① 지표 현재가에 기준가·등락률이 없고 timestamp 도 항상 null 이라 캔들의
  전일 종가로 등락률을 직접 계산했다 ② 미국 종목 현재가에도 기준가가 없어(랭킹에만 있다)
  SPY 도 같은 방식으로 통일했다 — 랭킹 상위 100 에 늘 있으리라 기대하지 않는 편이 낫다.
  국고채 3년은 토스 심볼 20가지가 전부 거절돼 뺐고, 정책금리로 대신했다.
- 2026-08-17 — **국내(DART) 사업보고서 분석 완성·배포.** 이제 국내·미국 양쪽 다 서술
  분석이 된다. DART 원문은 자체 XML 이고 `<SECTION-1>` 로 목차가 계층으로 들어 있어
  10-K 보다 추출이 쉬웠다 — 8종목 중 7종목이 첫 시도에 통과했다.
  걸린 곳은 KB금융 하나. ① 이름이 `[기재정정]사업보고서` 라 `startswith` 로 놓쳤고
  ② 그 정정본은 원문 파일이 아예 없었다(status 014). 후보를 최신순 3건까지 순서대로
  시도하게 고쳤다.
  **하루 상한을 국내·미국이 함께 쓰도록 바꿨다**(`llm_budget.py`) — 시장별로 따로 세면
  설정한 20건이 실제로는 40건이 된다. 상한은 지갑 기준이지 시장 기준이 아니다.
- 2026-08-17 — **6단계 서버 배포 완료.** 서버에서 AAPL 분석을 실제로 돌려 Anthropic 인증이
  서버 IP 에서도 통하는 것을 확인했다(토스와 달리 허용 IP 방식이 아니다). 공개 주소로
  인증 401 → 200 → 분석 조회까지 확인.
  배포 중 `deploy/update.sh` 의 결함이 드러났다 — 안내대로 `sudo -u stock` 으로 감싸 실행하면
  4단계 `sudo systemctl restart` 에서 멈춘다. `stock` 은 서비스 계정이라 sudo 비밀번호가 없다.
  스크립트를 고쳐 파일 작업만 stock 으로 넘기고 재시작은 실행 계정으로 하게 했다.
  **이제 서버에서 `cd /opt/stock && ./deploy/update.sh` 만 치면 된다.**
- 2026-08-17 — **현재가 API 버그 수정.** `symbols` 에 `min_length=6` 이 걸려 있어
  미국 티커 단건 조회(KO·AAPL)가 422 로 거절됐다. KRX 종목코드가 6자리라 맞는 값처럼
  보였고, 여러 종목이면 콤마 때문에 길어져 우연히 통과해서 늦게 드러났다.
- 2026-08-15 — **토스 에러 파싱 버그 수정.** 토큰 발급이 실패하면 `_to_error` 가
  `AttributeError` 로 죽어 진짜 원인이 가려졌다. 토스 토큰 엔드포인트는 OAuth2 표준대로
  `{"error": "문자열"}` 로 답하는데 코드는 객체만 가정했다. 고친 뒤에야
  `IP address not allowed (code=access_denied)` 가 보여서 원인이 분명해졌다.
  ※ 교훈: 에러 처리 코드가 죽으면 디버깅이 두 배로 어려워진다.
- 2026-08-18 — **수급 동향 완성·배포.** 국내 종목 상세에 투자자별 순매수 5거래일 표와
  공매도·신용·대차·프로그램 지표 4개가 붙었다. 토스가 다섯 경로로 다 주므로 새 키는 없다.
  설계에서 판단한 것 둘: ① 다섯 표를 그대로 늘어놓지 않고 "표 하나 + 지표 넷"으로
  압축했다 — 종목 상세가 이미 길어서 원자료를 나열하면 읽히지 않는다 ② **자료마다
  갱신 시각이 달라 기준일을 항목별로 따로 들었다** (공매도·대차는 당일 18~19시,
  신용·투자자별은 다음 영업일 04시). 하나의 날짜로 뭉뚱그리면 없는 자료를 있는 것처럼
  보이게 된다.
  다섯 중 하나가 실패해도 나머지는 보여준다. 없는 종목은 토스가 `stock-not-found` 라고
  말해 주므로 502(서버 오류)가 아니라 404 로 돌린다 — 원인을 잘못 짚게 하지 않는다.
  파싱 테스트 16건 추가(총 71건). 비율 필드가 소수로 와서 100을 곱하지 않으면
  "공매도 비중 0.06%" 처럼 그럴듯하게 틀린다.
- 2026-08-10 — 기획 문서 3종이 `D:\증권정보` 로 옮겨져 있던 것을 이 폴더로 복사해 되돌림.
  프로젝트 폴더는 `D:\d클로드 1\stock information` 하나로 확정.
  `D:\증권정보` 는 남아 있으나 쓰지 않는다. 나중에 지워도 된다.
