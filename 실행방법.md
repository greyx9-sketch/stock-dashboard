# 화면 띄우는 법

지금은 개발 중이라 **프로그램 두 개를 같이 켜 두어야** 화면이 나온다.
하나는 데이터를 주는 쪽(백엔드), 하나는 화면을 그리는 쪽(프론트엔드)이다.
나중에 서버에 올리면 이 과정은 사라지고 주소만 열면 된다.

---

## 준비 — 터미널 여는 법

1. 윈도우 검색창에 `PowerShell` 을 치고 **Windows PowerShell** 실행
2. 아래를 붙여넣고 Enter (프로젝트 폴더로 이동)

```powershell
cd "D:\d클로드 1\stock information"
```

---

## 1) 백엔드 켜기

터미널 **첫 번째 창**에서:

```powershell
cd "D:\d클로드 1\stock information\backend"
..\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000
```

`Application startup complete.` 가 보이면 켜진 것이다. **이 창은 닫지 않는다.**

## 2) 프론트엔드 켜기

터미널 **두 번째 창**을 새로 열고(PowerShell 을 하나 더 실행):

```powershell
cd "D:\d클로드 1\stock information\frontend"
npm run dev
```

`Local: http://localhost:5173/` 이 보이면 켜진 것이다. **이 창도 닫지 않는다.**

## 3) 화면 열기

브라우저 주소창에 입력:

```
http://localhost:5173
```

---

## 끄는 법

각 터미널 창에서 `Ctrl` + `C` 를 누른다. 창을 그냥 닫아도 된다.

---

## 데이터 채우기 — 평소에는 할 일 없음

확정 종가는 **자동으로 받아진다.** 백엔드만 켜 두면 된다.

- 평일 **오후 1시 20분**에 자동 수집 (확정 종가는 오후 1시 이후 공개된다)
- 백엔드를 **켤 때마다** 꺼져 있던 동안 빠진 날짜를 찾아 채운다

잘 돌고 있는지는 **화면 맨 아래 줄**에서 확인한다.
`자동 수집 정상 · 마지막 ... · 다음 수집 ...` 이라고 떠 있으면 정상이다.

### 직접 받고 싶을 때

기다리지 않고 지금 받거나, 더 과거까지 받고 싶을 때만 쓴다.

```powershell
cd "D:\d클로드 1\stock information"
.venv\Scripts\python.exe backend\scripts\ingest_krx.py
```

과거를 더 받으려면 뒤에 날짜 수를 붙인다(달력 기준). 예를 들어 1년치:

```powershell
.venv\Scripts\python.exe backend\scripts\ingest_krx.py 365
```

이미 받은 날은 건너뛴다. 여러 번 실행해도 문제 없다.

---

## 확인용 명령

| 하고 싶은 것 | 명령 |
| --- | --- |
| 토스증권 연결 확인 | `.venv\Scripts\python.exe backend\scripts\check_toss.py` |
| KRX 확정 종가 확인 | `.venv\Scripts\python.exe backend\scripts\check_krx.py` |
| API 문서 보기 (백엔드 켠 상태) | 브라우저에서 `http://localhost:8000/docs` |

---

## 잘 안 될 때

**화면은 뜨는데 숫자가 없고 오류가 보인다**
→ 백엔드(1번 창)가 꺼졌을 가능성이 높다. 그 창을 확인한다.

**데이터가 며칠째 갱신되지 않는다**
→ 화면 맨 아래 줄의 자동 수집 상태를 본다. `자동 수집 실패` 라고 떠 있으면
그 뒤에 이유가 함께 적혀 있다. 백엔드를 오후 1시 20분에 켜 두지 않았다면,
다시 켜는 것만으로 빠진 날짜가 채워진다.

**현재가가 "—" 로만 나온다**
→ 잠시 기다린다. 서버가 받아 오는 중이면 1~2초 안에 채워진다.
계속 비어 있으면 화면 상단 배지가 `현재가 연결 끊김` 인지 확인한다.

**`403` 오류가 난다**
→ 집 인터넷 공인 IP 가 바뀌었을 수 있다. 토스증권 웹에서
설정 > Open API > 허용 IP 관리 에 현재 IP 가 등록돼 있는지 확인한다.

**`npm` 또는 `python` 을 찾을 수 없다고 한다**
→ 폴더 경로가 틀렸을 가능성이 높다. 위의 `cd` 명령부터 다시 실행한다.
