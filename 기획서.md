# 개인용 증권 정보 웹페이지 구축 기획서

작성일: 2026-08-07 / 상태: v0.5

---

## 1. 이 문서의 전제

"나만의 증권사 웹페이지"를 실제 증권사처럼 매매까지 붙이는 서비스가 아니라, **본인과 소수의 지인이 보는 시장 정보 대시보드**로 정의한다. 실시간 시세, 주요 일정과 결과, 기업 분석 자료를 한 화면에서 확인하는 것이 목적이다. **매매 주문과 계좌·자산 조회는 범위에서 완전히 제외한다** — 읽기 전용 정보 사이트다.

불특정 다수 대상 공개 서비스가 아니므로, 상용 서비스 수준의 가용성·SLA·다중 사용자 권한관리는 설계 목표에서 제외한다. 이 결정이 아키텍처를 상당히 단순하게 만든다.

---

## 2. 운영 형태

접속 대상은 본인, 지인 소수, 그리고 **입사지원 시 이력서에 링크를 첨부해 열람하게 될 채용담당자**다. 마지막 항목이 이 프로젝트의 성격을 상당히 바꾼다. 개인 도구였다면 넘어갈 만한 것들이 포트폴리오로서는 결함이 되기 때문이다.

인증은 공용 비밀번호 한 개로 간다. 계정 관리 시스템을 만들 필요 없이, 비밀번호를 입력하면 세션 쿠키를 발급하는 정도면 충분하다. 이력서에는 URL과 비밀번호를 함께 적는다. 여기에 `robots.txt`로 검색엔진 색인만 막아두면 운영상 필요한 조치는 끝난다.

### 채용담당자 열람을 전제로 할 때 반드시 반영해야 할 것

**계좌·자산·주문 API는 아예 쓰지 않는다.** 이 사이트는 정보를 받아 보여주기만 하는 조회 전용이다. 보유 종목이나 평가금액이 화면에 뜰 일이 없으므로 자산 노출 위험 자체가 사라지고, `X-Tossinvest-Account` 헤더도 필요 없어진다. 액세스 토큰 하나로 모든 호출이 끝난다. 보안 설계 부담이 크게 줄어드는 결정이다.

**장 마감 후와 주말에도 화면이 살아 있어야 한다.** 채용담당자가 언제 접속할지 알 수 없고, 통계적으로는 평일 야간이나 주말일 가능성이 높다. 실시간 시세만 믿고 만들면 그 시간대에는 빈 화면처럼 보인다. 장 외 시간에는 최종 종가와 "장 마감" 표시를 명확히 띄우고, 차트·재무·기업분석처럼 시간과 무관한 콘텐츠가 화면의 주축이 되도록 구성한다. 10-K 분석 모듈이 여기서 큰 역할을 한다.

**첫 화면에서 무엇을 만들었는지 30초 안에 파악되어야 한다.** 채용담당자는 대시보드를 오래 들여다보지 않는다. 로그인 직후 또는 별도의 `/about` 페이지에 프로젝트 개요, 사용 기술, 데이터 소스, 아키텍처 다이어그램, 직접 구현한 부분을 한 장으로 정리해 둔다. 이 페이지가 사실상 이력서의 연장이며, 대시보드 자체보다 평가에 더 크게 작용할 수 있다.

**죽어 있으면 안 된다.** 지원 후 몇 주간은 살아 있어야 하므로 상시 구동이 전제다. 최소한의 헬스체크와, 외부 API 장애 시에도 캐시된 데이터로 화면이 뜨는 폴백을 넣어둔다. 지원자가 링크를 걸었는데 500 에러가 뜨는 것이 최악의 시나리오다.

## 3. 데이터 소스 구성

한 곳에서 다 받을 수 없다. 성격별로 나눠 붙이는 것이 현실적이다.

### 3.1 시세 — 토스증권 Open API (확정)

토스증권 Open API(`https://openapi.tossinvest.com`)로 확정. 국내(KRX)와 미국 주식을 하나의 API로 다룰 수 있어 이 프로젝트의 범위와 정확히 맞는다.

인증은 OAuth 2.0 Client Credentials Grant다. 토스증권 WTS에 로그인해 설정 > Open API 메뉴에서 `client_id`와 `client_secret`을 발급받고, `POST /oauth2/token`으로 액세스 토큰을 받아 `Authorization: Bearer {token}` 헤더에 실어 호출한다. 계좌·자산·주문 계열 API는 `X-Tossinvest-Account` 헤더가 추가로 필요하다. 계좌·자산·주문은 이 프로젝트에서 쓰지 않기로 했으므로 토큰만 있으면 된다.

제공 범위는 시세(현재가·호가·체결·캔들·상하한가), 종목 마스터, 매수 유의사항(정리매매·과열·투자경고·VI), 수급 동향(투자자별 매매동향·프로그램매매·공매도·신용거래·대차거래, 국내 종목 한정), 환율, 장 운영 캘린더(KRX·NXT·미국), 랭킹, 시장 지표(국내 지수·국채)다. 수급 동향과 랭킹은 예상 밖의 수확이라 화면 기획에 반영할 가치가 있다.

**중요한 제약 두 가지가 있다.**

첫째, **토스증권 Open API는 현재 REST만 제공한다.** WebSocket이 없다. 따라서 "실시간"은 서버가 일정 주기로 폴링해 캐시를 갱신하고, 그 캐시를 프론트엔드로 밀어주는 구조로 구현해야 한다. 앞선 초안의 웹소켓 중계 설계는 폐기하고 아래 4번처럼 바꾼다.

둘째, **허용 IP 등록이 필수다.** 설정 > Open API 메뉴 하단의 허용 IP 관리에 등록되지 않은 IP에서의 호출은 403으로 차단된다. 이 때문에 서버는 고정 IP를 가져야 하고, 개발 단계에서 집이나 회사에서 호출하려면 해당 IP도 등록해 두어야 한다. **VPS를 먼저 확보하고 그 IP를 등록하는 것이 순서상 맞다.** 유동 IP 환경에서 개발하다가 IP가 바뀌면 원인을 모른 채 403만 보게 된다.

Rate limit은 클라이언트 × API 그룹 단위 초당 요청 수(TPS)로 걸린다. 주요 그룹의 한도는 아래와 같다.

| 그룹 | 한도 | 이 프로젝트에서 쓰는 API |
|---|---|---|
| `AUTH` | 초당 5회 | 토큰 발급 |
| `MARKET_DATA` | 초당 10회 | 현재가, 호가, 체결, 상하한가 |
| `MARKET_DATA_CHART` | 초당 5회 | 캔들 |
| `STOCK` | 초당 5회 | 종목 마스터, 유의사항 |
| `STOCK_TRADING_TREND` | 초당 10회 | 수급 동향 |
| `MARKET_INFO` | 초당 3회 | 환율, 장 운영 캘린더 |
| `RANKING` | 초당 5회 | 랭킹 |
| `MARKET_INDICATOR_PRICE` | 초당 10회 | 지수·국채 현재가 |

이 한도는 사전 공지 없이 조정될 수 있고, 현재 한도는 응답 헤더 `X-RateLimit-Limit`으로 확인할 수 있다. 429를 받으면 `Retry-After` 헤더 값만큼 대기한 뒤 지수 백오프와 jitter를 적용해 재시도한다. `X-RateLimit-Remaining`이 줄어들면 선제적으로 속도를 늦추는 로직을 넣어두면 안전하다.

에러는 `{"error": {"requestId", "code", "message", "data"}}` 형태의 단일 envelope으로 내려온다. `requestId`는 응답 헤더 `X-Request-Id`와 같은 값이며 CS 문의 시 첨부한다. 초기 개발에서 자주 만날 코드는 `invalid-token`(토큰 무효), `expired-token`(만료), `edge-blocked`(Authorization 헤더 누락 또는 허용되지 않은 요청), `stock-not-found`, `rate-limit-exceeded` 정도다.

### 3.2 공시·재무 — OpenDART

금융감독원 OpenDART(opendart.fss.or.kr). DART에 공시되는 보고서 원문을 XML로 받을 수 있고, 사업보고서 주요항목과 주요재무계정, 지분보고서, 주요사항보고서, 증권신고서 종합정보를 데이터 형식으로 바로 활용할 수 있다. 인증키 신청만 하면 개인도 무료로 쓸 수 있다.

기업 분석 화면의 재무 데이터, 공시 타임라인, 지분 변동 알림이 전부 여기서 나온다. 실질적으로 이 프로젝트의 핵심 데이터 소스다.

### 3.3 종목 마스터·통계 — KRX Open API / 공공데이터포털

KRX Data Marketplace의 OPEN API(openapi.krx.co.kr)와 공공데이터포털의 금융위원회 API를 보조로 쓴다. 다만 공공데이터포털 쪽은 실시간이 아니다. 금융위원회가 제공하는 서비스는 데이터 갱신이 기준일자로부터 영업일 하루 뒤 오후 1시 이후에 이루어져, 금요일 데이터는 다음 주 월요일에 제공된다. 종목 마스터, 상장 정보, 과거 일별 데이터 확보용으로만 쓰고 시세는 기대하지 않는다.

### 3.4 매크로 지표 — ECOS / FRED

한국은행 ECOS 오픈API와 세인트루이스 연준 FRED API. 금리, 환율, CPI, 유가 등을 받아 대시보드 상단 매크로 스트립에 쓴다. 이미 별도로 관리 중인 매크로 정리 엑셀과 지표 항목을 맞춰두면 작업이 중복되지 않는다.

### 3.5 미국 주식

토스증권 API가 미국 주식 시세와 미국 장 운영 캘린더(데이마켓·프리마켓·정규장·애프터마켓)를 함께 제공하므로 별도 소스가 필요 없다. 다만 수급 동향(투자자별 매매동향, 프로그램매매, 공매도, 신용거래, 대차거래)은 국내 종목에만 제공되므로 미국 종목 화면은 시세·차트·기업분석 중심으로 구성한다. 미국 기업의 재무와 서술 분석은 6번의 SEC EDGAR 모듈이 담당한다.

### 3.6 일정 데이터 — 가장 까다로운 부분

실적발표 일정, IPO/공모 일정, 배당 기준일, FOMC·금통위 일정은 단일 API로 깔끔하게 제공되지 않는다. 접근법은 셋이다. 첫째, DART의 "연결재무제표 기준 영업실적 등에 대한 전망" 등 공시를 파싱해 사후적으로 채우는 방법. 둘째, 거래소 공시 채널에서 기준일 관련 공시를 긁는 방법. 셋째, FOMC·금통위처럼 연간 확정 일정은 수기로 한 번 입력해두고 매년 갱신하는 방법.

**1단계에서는 수기 입력 + 캘린더 UI만 만들고, 자동 수집은 2단계로 미루는 것을 권한다.** 여기서 완벽을 추구하면 프로젝트가 여기서 멈춘다.

---

## 4. 시스템 구조

토스증권이 REST만 제공하므로, "실시간"은 서버 폴링으로 만든다.

```
[배치 워커]  ──REST──▶  OpenDART, SEC EDGAR, ECOS, FRED
   (일 1회)                재무·공시·매크로 → DB 적재

[시세 폴러]  ──REST──▶  토스증권 Open API
   (장중 1~3초 주기)       관심종목 현재가·호가 → 메모리 캐시
        │
        ▼
 [백엔드 API 서버 (FastAPI)]
   토큰 발급·갱신, rate limit 제어, 캐시 관리
        │
        ├── SSE 또는 WebSocket ──▶ 프론트엔드 (시세 push)
        └── REST ────────────────▶ 프론트엔드 (재무·공시·분석 조회)
```

핵심 원칙 셋을 지킨다.

**브라우저가 토스증권 API를 직접 부르지 않는다.** `client_secret`이 노출되고, 허용 IP 방식이라 애초에 브라우저에서는 호출이 막힌다. 모든 외부 호출은 서버에서만 나간다.

**폴링은 서버가 한 번만 한다.** 접속자가 1명이든 20명이든 토스증권으로 나가는 요청 수는 동일하다. 서버가 관심종목 시세를 주기적으로 갱신해 메모리에 두고, 프론트엔드에는 SSE(Server-Sent Events)로 밀어준다. SSE는 단방향이라 WebSocket보다 구현이 단순하고 이 용도에는 충분하다.

**폴링 주기는 rate limit에서 역산한다.** `MARKET_DATA`가 초당 10회이므로, 현재가 API가 복수 종목을 한 번에 조회할 수 있다면 관심종목 수십 개를 1초 주기로도 돌릴 수 있다. 단건 조회만 가능하다면 종목 수에 따라 주기를 늘려야 한다. 이 부분은 개발 착수 시 API 레퍼런스에서 `symbols` 파라미터가 복수를 받는지 먼저 확인한다. 어느 쪽이든 여유를 두고 시작해 필요할 때 조이는 편이 안전하다.

장 마감 후에는 폴링을 멈추고 마지막 종가를 유지한다. 장 운영 캘린더 API(`/api/v1/market-calendar/KR`, `/US`)로 세션 시간을 받아 폴러를 켜고 끄면 휴장일 처리도 자동으로 된다. 이 API가 있다는 것 자체가 상당한 편의다.

### 기술 스택

백엔드는 **Python + FastAPI**. DB는 **SQLite**로 시작한다. 단일 서버에 데이터량도 수백 MB 수준이라 충분하고 파일 하나라 백업이 쉽다. 시계열이 커지면 PostgreSQL로 옮긴다.

프론트엔드는 **React + Vite + TailwindCSS**, 차트는 **lightweight-charts**(TradingView 오픈소스)나 ECharts를 쓴다. 캔들차트는 직접 그리지 않는다.

배포는 **소형 VPS 상시 구동**. 월 5~10달러대의 Vultr, Linode, 또는 Oracle Cloud 무료 티어면 충분하다. 고정 IP가 반드시 필요하므로(토스 허용 IP 등록) VPS 선정 시 이 조건을 먼저 확인한다. 도메인 하나 붙이고 Caddy로 HTTPS를 잡으면 된다.

### 클로드 코드로 개발할 때의 작업 방식

프레임워크 이름을 몰라도 문제되지 않는다. 위 스택은 클로드 코드에게 "이 구성으로 만들어달라"고 전달하기 위한 지정어이지, 직접 익혀야 할 대상이 아니다. 다만 도구가 코드를 써준다고 해서 사람이 할 일이 없어지는 것은 아니고, **남는 일은 셋이다.**

첫째, **요구사항을 잘게 쪼개서 준다.** "증권 웹사이트 만들어줘"는 실패한다. "토스증권 API로 종목 하나의 현재가를 받아 콘솔에 출력하는 파이썬 스크립트를 만들어달라"부터 시작해서, 되는 것을 확인하고 다음 단계로 넘어간다. 한 번에 하나씩이 원칙이다.

둘째, **결과를 검증한다.** 화면에 숫자가 뜬다고 맞는 값이 아니다. 토스증권 앱에 뜨는 값과 대조해 보는 습관이 필요하다. 특히 등락률, 거래대금 단위, 미국 주식의 환율 환산은 틀리기 쉬운 지점이다.

셋째, **키를 관리한다.** `client_id`, `client_secret`, OpenDART 인증키, Anthropic API 키는 전부 `.env` 파일에 넣고 `.gitignore`에 `.env`를 등록한다. 이걸 깜빡하고 GitHub에 올리면 키가 공개된다. 포트폴리오 목적으로 저장소를 공개할 계획이라면 특히 중요하다. 프로젝트 첫날 `.gitignore`부터 만드는 것을 권한다.

작업 순서로는 Git 저장소를 먼저 만들고 커밋을 자주 남기는 것이 좋다. 클로드 코드가 여러 파일을 한꺼번에 고치다 보면 잘 돌던 상태로 되돌아가야 할 때가 오는데, 커밋이 없으면 되돌릴 곳이 없다.

## 5. 화면 구성

### 5.1 메인 대시보드
상단에 매크로 스트립(코스피/코스닥/S&P500/원달러/국고3년/WTI). 그 아래 관심종목 그리드 — 현재가, 등락률, 거래량, 전일비를 실시간 갱신. 우측에 오늘의 공시 피드와 다가오는 일정 3~5건.

### 5.2 종목 상세
가격 차트(일/주/월, 이동평균), 호가창, 기업 개요, DART 기반 재무 요약(매출·영업이익·순이익 5개년, 부채비율, ROE), 밸류에이션(PER/PBR/배당수익률), 해당 종목 공시 타임라인, 본인 메모.

**메모 기능이 이 프로젝트의 차별점이다.** 증권사 HTS에는 없고, 텔레그램 채널에 쓰던 코멘트를 종목별로 축적하면 시간이 지날수록 자산이 된다.

### 5.3 캘린더
월간 뷰에 실적발표, 배당 기준일, 공모주 청약, FOMC·금통위, 옵션만기를 종류별 색으로 표시. 클릭하면 관련 종목/자료로 이동.

### 5.4 기업 분석
동종업계 비교 테이블(같은 업종 종목의 밸류에이션·성장률 나열), 재무제표 추이 차트, 스크리너(PER 15배 이하 + ROE 10% 이상 같은 조건 필터).

### 5.5 소개 페이지
프로젝트 개요, 사용 기술, 데이터 소스, 아키텍처 다이어그램, 직접 구현한 부분. 채용담당자를 위한 페이지이자 본인의 작업 기록이다. 대시보드보다 먼저 만들어도 좋을 만큼 비중이 있다.

---

## 6. 해외 10-K 분석 모듈

티커를 입력하면 해당 기업의 전반적인 그림 — 무슨 회사인지, 어떻게 돈을 버는지, 최근 무슨 일이 있었는지, 약점과 강점은 무엇인지, 경쟁사 대비 어디에 서 있는지 — 이 한 페이지에 정리되어 나오는 기능이다. 이 프로젝트에서 가장 만들 가치가 큰 부분이자, 가장 어려운 부분이다.

### 6.1 데이터 확보 — SEC EDGAR

미국 상장사 공시는 SEC EDGAR에서 전부 무료로 받을 수 있다. API 키도 등록도 필요 없고, 요구되는 것은 신원을 밝히는 User-Agent 헤더 하나뿐이다.

쓸 엔드포인트는 넷이다.

`company_tickers.json`은 티커와 회사명을 EDGAR 내부 식별자인 CIK로 매핑해준다. EDGAR는 티커로 색인되어 있지 않고 CIK를 쓰므로, 티커 검색을 지원하려면 이 파일을 먼저 받아 DB에 적재해야 한다. CIK는 10자리로 zero-padding해서 URL에 넣는다.

`https://data.sec.gov/submissions/CIK##########.json`은 해당 기업의 전체 공시 이력을 준다. 폼 종류, accession number, 제출일이 들어 있어 "이 회사의 모든 10-K 목록"을 뽑는 진입점이 된다.

`https://data.sec.gov/api/xbrl/companyfacts/CIK##########.json`은 그 기업이 XBRL로 태깅해 제출한 모든 재무 사실을 한 번에 준다. 매출, 총자산, 발행주식수 등 수백 개 항목이 taxonomy·concept·기간·폼별로 정리되어 온다. 응답이 무겁지만 재무 시계열을 통째로 확보할 수 있다.

`https://data.sec.gov/api/xbrl/frames/us-gaap/{CONCEPT}/{UNIT}/CY{YEAR}.json`은 축을 뒤집어 하나의 개념을 특정 기간의 전 기업에 대해 준다. 경쟁사 비교와 스크리닝에 쓰는 엔드포인트다.

여기에 더해 `efts.sec.gov`의 전문검색이 2001년 이후 모든 공시 본문과 첨부를 키워드로 검색해준다. 특정 표현이 언제부터 등장했는지 추적하는 용도로 유용하다.

**접근 규칙 세 가지를 지키지 않으면 막힌다.** 첫째, User-Agent 헤더에 이름과 연락 가능한 이메일을 넣는다. 이게 없으면 403이 뜨고, 403의 가장 흔한 원인이다. 둘째, CIK는 10자리 zero-padding. 셋째, 초당 10건 이하로 호출한다. SEC는 사용하는 장비 수와 무관하게 사용자당 초당 10건을 상한으로 두고, 초과하면 해당 IP를 일정 시간 차단한다. 차단 상태에서 즉시 재시도하면 차단이 길어진다.

실무적으로는 8 req/s 정도로 여유를 두고, 요청 사이에 100ms 지연을 넣고, 캐싱을 건다. 캐시 정책은 성격에 따라 다르게 잡는다. 개별 공시 문서는 한 번 발행되면 변하지 않으므로 영구 캐싱, companyfacts는 24시간, submissions는 6~12시간, 전문검색 결과는 1시간 정도가 적정선이다.

전 종목을 훑을 계획이라면 개별 API를 만 번 넘게 호출하지 말고 SEC가 제공하는 벌크 파일(`sec.gov/files/`의 `companyfacts.zip`, `submissions.zip`, 일 1회 갱신)을 한 번 받아 초기 적재한 뒤, 이후 증분만 API로 따라가는 방식이 정석이다.

### 6.2 신규 10-K 자동 감지

EDGAR의 daily index 또는 회사별 RSS 피드를 매일 새벽에 폴링해서, 관심 유니버스에 속한 CIK의 신규 10-K가 뜨면 큐에 넣는다. 큐에 들어간 건은 원문 다운로드 → 섹션 분할 → 분석 → DB 저장까지 자동으로 흐른다. 10-K는 연 1회이므로 큐가 붐빌 일은 없고, 오히려 분기 실적(10-Q)과 수시공시(8-K)가 더 자주 들어온다.

### 6.3 10-K 문서 파싱

10-K 원문은 그대로 다루기엔 크다. 대형주 기준 수십만 토큰에 달하고, HTML 구조도 회사마다 제각각이다. 통째로 LLM에 던지는 접근은 비용과 품질 양쪽에서 실패한다.

따라서 **섹션 분할이 이 모듈의 실질적 난이도 대부분을 차지한다.** 다행히 10-K는 표준 Item 구조를 따르므로 정규식과 목차 파싱을 조합하면 대부분 잡힌다.

| Item | 내용 | 화면에서 쓰이는 곳 |
|---|---|---|
| Item 1 | Business | 회사 개요, 사업구조, 경쟁 환경 |
| Item 1A | Risk Factors | 리스크·문제점 |
| Item 3 | Legal Proceedings | 소송·규제 이슈 |
| Item 7 | MD&A | 최근 이슈, 경영진 시각, 실적 해석 |
| Item 7A | Market Risk | 금리·환율·원자재 노출 |
| Item 8 | Financial Statements | 재무 (단, 수치는 XBRL에서 별도 확보) |

### 6.4 분석 계층 — LLM

분할된 섹션별로 LLM(Claude API)에 요약을 시키고, 그 결과를 다시 합쳐 하나의 기업 프로파일로 만드는 map-reduce 구조로 간다. 출력은 자유 서술이 아니라 **고정된 JSON 스키마**로 받아야 화면에 붙일 수 있다.

스키마 초안은 회사 한 줄 정의, 사업 세그먼트 목록(명칭·설명·매출비중), 수익 모델, 주요 고객·공급망 의존도, 경쟁사 목록, 강점 3~5개, 리스크 3~5개(각각 심각도와 근거 섹션), 최근 1년 주요 변화, 경영진이 강조한 전략 방향 정도로 잡는다.

**여기서 반드시 지켜야 할 원칙이 하나 있다. 숫자는 LLM에게 시키지 않는다.** 매출, 이익, 마진, 세그먼트별 금액은 전부 XBRL에서 직접 가져오고, LLM은 서술형 해석만 담당한다. LLM이 10-K 본문에서 숫자를 읽어 요약하게 두면 반드시 틀린 값이 섞이고, 그게 섞이면 이 대시보드 전체를 신뢰할 수 없게 된다.

같은 이유로 모든 서술 항목에 **출처 섹션 앵커를 함께 저장**한다. "규제 리스크가 크다"는 문장 옆에 Item 1A의 해당 문단으로 가는 링크가 붙어야, 나중에 본인이 검증할 수 있다.

분석 결과는 DB에 캐싱한다. 한 기업의 10-K 하나는 한 번만 분석하면 되고, 새 10-K가 나올 때만 재실행한다. 이렇게 하면 API 비용이 관리 가능한 수준으로 떨어진다.

### 6.5 최신성 보완

10-K는 연 1회 문서라 그것만으로는 "최근 이슈"를 못 채운다. 세 가지를 덧붙인다. 8-K(수시공시)로 인수합병·경영진 변동·중대 사건을 잡고, 10-Q로 분기 실적 흐름을 따라가고, 뉴스 소스로 시장 반응을 채운다. 화면에서는 "연간 구조(10-K 기반)"와 "최근 동향(8-K·10-Q·뉴스 기반)"을 시각적으로 구분해 표시해야 사용자가 정보의 신선도를 오해하지 않는다.

### 6.6 경쟁사 비교

가장 자동화가 어려운 부분이다. 접근법 셋을 조합한다. SIC 코드로 1차 후보군을 뽑고, Item 1 본문에서 회사가 직접 언급한 경쟁사 이름을 추출하고, 그래도 어색하면 수기로 보정한다. 완전 자동은 기대하지 않는 편이 낫다. 실무적으로는 자동 추천 목록을 띄우고 본인이 체크박스로 확정하는 반자동 방식이 가장 빠르다.

peer가 확정되면 frames API로 동일 개념을 횡단 조회해 매출성장률, 영업이익률, PER, PBR, ROE를 나란히 놓는다.

### 6.7 시각화 구성

한 페이지 리포트의 구성 순서는 이렇다. 상단에 주가 차트와 핵심 지표 카드. 그 아래 사업 세그먼트 도넛차트와 세그먼트별 5개년 매출 스택 바. 이어서 매출·영업이익·순이익 추이 라인차트와 마진 추이. 그다음 경쟁사 비교 — 밸류에이션 대 성장률 산점도에 본인 종목을 강조 표시. 리스크는 카테고리별(규제/경쟁/공급망/재무/기술)로 묶어 심각도 히트맵. 마지막에 공시 타임라인과 서술형 요약 카드.

차트는 전부 XBRL 수치 기반이므로 데이터만 들어오면 자동으로 그려진다. 서술 카드만 LLM 출력이다.

### 6.8 비용 설계 — "모든 기업"을 어떻게 감당할 것인가

모든 미국 상장사를 미리 분석해 두는 방식은 비용이 크다. 다만 **화면의 대부분은 LLM 없이 채울 수 있다**는 점이 해법의 출발점이다.

XBRL 기반 데이터 — 매출·이익 추이, 마진, 세그먼트별 금액, PER·PBR·ROE, 경쟁사 비교 테이블, 각종 차트 — 는 EDGAR에서 받아 계산하는 것이므로 **LLM 비용이 0이다.** 이것만으로 기업 상세 화면의 7할이 채워진다. LLM이 필요한 부분은 "무슨 회사인지, 강점과 리스크가 무엇인지" 같은 서술형 요약뿐이다.

그래서 3단 구조로 간다.

**Tier C — 전 종목, 즉시, 무료.** 모든 미국 상장사에 대해 XBRL 재무·차트·밸류에이션·peer 비교를 제공한다. `companyfacts.zip` 벌크 파일을 받아 DB에 넣어두면 어떤 티커를 쳐도 즉시 나온다. LLM 호출 없음.

**Tier A — 사전 분석, 배치.** S&P 500과 본인 관심 종목 정도(수백 개)는 미리 서술 분석을 돌려 저장한다. 조회 즉시 완성된 리포트가 뜬다.

**Tier B — 온디맨드 + 영구 캐시.** 그 외 티커는 처음 조회될 때 그 자리에서 분석한다(수십 초 소요, 로딩 표시). 결과는 DB에 영구 저장하므로 두 번째 조회부터는 즉시 뜬다. 10-K는 연 1회 문서라 한 번 분석하면 1년간 유효하다.

이 구조면 "모든 기업을 볼 수 있다"는 요구와 비용 통제가 동시에 만족된다. 실제로 본인과 지인 20명이 1년간 조회할 기업 수는 수백 개를 넘기 어렵고, Tier B의 실비는 사실상 무시할 수준이 된다.

#### 비용 추산

2026년 8월 기준 Claude API 요금은 Haiku 4.5가 100만 토큰당 입력 $1 / 출력 $5, Sonnet 5가 입력 $2 / 출력 $10(2026년 8월 31일까지의 도입가, 9월 1일부터 $3 / $15)이다. Batch API를 쓰면 입력·출력 모두 50% 할인된다. 10-K 분석은 실시간성이 필요 없으므로 Tier A 배치는 반드시 Batch API로 돌린다.

기업 하나당 Item 1·1A·7을 추려 입력 15만 토큰, 출력 4천 토큰으로 잡으면 대략 이렇다.

| 모델 | 기업 1건 | S&P 500 | 미국 상장 전체(약 6,000) |
|---|---|---|---|
| Haiku 4.5 (배치) | 약 $0.09 | 약 $45 | 약 $520 |
| Haiku 4.5 (표준) | 약 $0.17 | 약 $90 | 약 $1,040 |
| Sonnet 5 (배치) | 약 $0.17 | 약 $85 | 약 $1,020 |
| Sonnet 5 (표준) | 약 $0.34 | 약 $170 | 약 $2,040 |

전 종목 사전 분석은 Haiku 배치로도 60만 원대다. **하지 않는다.** Tier A를 S&P 500으로 한정하면 6만 원 안쪽이고, 10-K가 연 1회 갱신되므로 이 금액이 곧 연간 비용에 가깝다. 개인 프로젝트로 감당 가능한 수준이다.

추가 절감 수단은 넷이다. 첫째, 모델 라우팅 — 1차 요약은 Haiku로 돌리고, 본인이 실제로 깊게 보는 종목만 Sonnet으로 재분석한다. 둘째, 섹션 선별 — Item 1·1A·7만 넣고 나머지는 버린다. 이것만으로 입력 토큰이 절반 이하로 떨어진다. 셋째, 사전 압축 — 리스크 섹션은 소제목과 첫 문단만 추출해 넣는다. 10-K의 Item 1A는 정형화된 법률 문구가 많아 압축 효율이 높다. 넷째, 프롬프트 캐싱 — 문서 본문은 매번 다르므로 효과가 제한적이지만, 긴 시스템 프롬프트와 출력 스키마는 캐싱해 둘 수 있다.

마지막으로 **Console에서 월 사용량 알림과 상한을 설정해 둔다.** 코드 버그로 같은 문서를 반복 분석하는 사고가 개인 프로젝트에서 가장 흔한 비용 사고다.

### 6.9 "아무것도 안 해도 되는가"에 대해

수집과 분석 자체는 완전 자동화가 가능하다. 새 10-K가 뜨면 알아서 받아 분석하고 화면이 갱신되는 구조까지 갈 수 있다.

다만 **초기 구축 후에도 사람 손이 남는 지점이 셋 있다.** 첫째, 섹션 파싱은 회사별 문서 구조 편차 때문에 예외 케이스가 계속 나온다. 파싱 실패 건을 로그로 남기고 주기적으로 손보는 작업이 필요하다. 둘째, peer 매핑은 앞서 말한 대로 반자동이다. 셋째, 프롬프트는 처음 한 번에 만족스럽게 나오지 않는다. 실제 결과물을 보면서 몇 차례 다듬는 과정이 반드시 들어간다.

이 셋을 초기에 집중적으로 정리해두면, 이후에는 방치해도 돌아가는 상태에 도달한다. "아무것도 안 하는" 상태는 목표로 삼을 만하지만, 처음부터 그렇게 되지는 않는다는 전제로 일정을 잡는 것이 좋다.

---

## 7. 단계별 로드맵

**0단계 — 준비 (1~2주)**
토스증권 계좌 개설 및 WTS에서 `client_id`·`client_secret` 발급, VPS 확보 후 해당 IP를 허용 IP에 등록, OpenDART 인증키 발급, Git 저장소와 `.gitignore` 생성. 파이썬 스크립트로 삼성전자 현재가를 한 번 찍어보는 것까지가 완료 조건이다. **허용 IP 등록 때문에 VPS를 이 단계에서 먼저 확보해야 한다**는 점이 이 프로젝트의 특이사항이다.

**1단계 — 데이터 파이프라인 (3~4주)**
종목 마스터 적재, DART 공시·재무 수집 배치, 일별 시세 적재, DB 스키마 확정. 화면은 아직 없고 DB에 데이터가 제대로 쌓이는지만 확인한다. 지루하지만 여기가 부실하면 나중에 전부 다시 짜야 한다.

**2단계 — 정적 웹 화면 (3~4주)**
FastAPI로 조회 API를 열고 React로 종목 상세와 관심종목 리스트를 만든다. 아직 실시간 없이 DB 조회만. 차트, 재무 테이블, 공시 목록까지.

**3단계 — 실시간 연결 (2~3주)**
증권사 웹소켓을 백엔드에 붙이고 프론트로 중계. 토큰 자동 갱신, 재연결, 장 시작·종료 처리, 휴장일 처리를 여기서 다 잡는다.

**4단계 — 10-K 분석 모듈 (4~6주)**
EDGAR 수집 파이프라인, 섹션 파서, LLM 분석 계층, 기업 리포트 화면. 국내 대시보드와 독립적으로 만들 수 있는 영역이라, 국내 쪽이 막히면 이쪽을 먼저 해도 무방하다. 오히려 EDGAR는 API 신청 절차가 없어 당장 시작할 수 있다는 점에서, 0단계 승인 대기 기간에 착수하기 좋다.

**5단계 — 분석 기능 (지속)**
캘린더, 스크리너, 종목 메모, 알림. 여기서부터는 끝이 없으므로 우선순위를 정해 하나씩 붙인다.

전체적으로 **주말과 퇴근 후 시간만 쓴다는 가정에서 5~6개월** 규모다. 2단계까지만 완성해도 충분히 쓸 만한 물건이 나오고, 4단계 10-K 모듈은 그 자체로 독립적인 가치가 있다.

---

## 8. 예상되는 기술적 난관

**토큰 관리.** 액세스 토큰은 유효기간이 있다. 파일이나 DB에 캐싱하고 만료 시점을 계산해 재발급하는 로직을 처음부터 제대로 짠다. 개발 중 서버를 재시작할 때마다 토큰을 새로 받는 구조로 만들면 `AUTH` 그룹 한도(초당 5회)와는 별개로 불필요한 발급이 쌓인다. 401 `expired-token`을 받으면 자동으로 재발급 후 원래 요청을 한 번 재시도하는 래퍼를 만들어두면 이후 모든 코드가 편해진다.

**허용 IP.** 403 `edge-blocked`의 상당수가 여기서 나온다. VPS IP, 개발용 IP를 모두 등록했는지 먼저 확인하는 것이 디버깅 1순위다.

**Rate limit 제어.** API 그룹별로 한도가 다르므로 그룹 단위 토큰 버킷을 하나씩 두고 그 뒤에서만 호출하는 구조로 만든다. 응답 헤더의 `X-RateLimit-Remaining`을 읽어 선제적으로 속도를 늦추고, 429에서는 `Retry-After`를 지킨다.

**장 운영시간과 세션.** 국내는 KRX와 NXT의 세션이 다르고, 미국은 데이마켓·프리마켓·정규장·애프터마켓이 나뉜다. 장 운영 캘린더 API로 받아 폴러의 on/off와 화면 표시("정규장", "애프터마켓", "장 마감")를 제어한다. 직접 시간표를 하드코딩하면 휴장일에 반드시 틀린다.

**미국 종목의 환율 환산.** 원화 표시를 넣을 생각이면 환율 API를 함께 물려야 하고, 어느 시점 환율로 환산했는지 화면에 명시해야 한다. 여기서 값이 어긋나면 채용담당자 눈에 가장 먼저 띈다.

**DART 재무데이터의 비정형성.** 계정과목명이 회사마다 다르고 연결/별도 구분도 섞인다. XBRL 표준계정 코드 기준 매핑 테이블을 만들어야 비교가 가능해진다. 국내 기업 분석 기능의 실질적 난이도 대부분이 여기 있다.

**EDGAR 섹션 파싱.** 6.3에서 다룬 대로 회사별 문서 구조 편차가 크다. 파싱 실패를 조용히 넘기지 말고 실패 로그를 남겨 주기적으로 손본다.

---

## 9. 데이터 모델 초안

| 테이블 | 주요 컬럼 |
|---|---|
| `stocks` | 종목코드, ISIN, 종목명, 시장구분, 업종, 상장일, 상장주식수 |
| `daily_prices` | 종목코드, 일자, 시가/고가/저가/종가, 거래량, 거래대금 |
| `financials` | 종목코드, 사업연도, 보고서구분, 계정코드, 계정명, 당기금액, 전기금액 |
| `disclosures` | 접수번호, 종목코드, 공시일, 보고서명, 제출인, 원문URL |
| `events` | 일자, 구분(실적/배당/공모/매크로), 종목코드(nullable), 제목, 비고 |
| `macro_series` | 지표코드, 지표명, 출처, 기준일, 값 |
| `watchlist` | 그룹명, 종목코드, 정렬순서 |
| `notes` | 종목코드, 작성일, 본문, 태그 |
| `sec_companies` | CIK, 티커, 회사명, SIC코드, 거래소 |
| `sec_filings` | accession, CIK, 폼종류, 제출일, 회계연도, 원문URL, 처리상태 |
| `filing_sections` | accession, Item번호, 섹션명, 본문, 문자수 |
| `company_profiles` | CIK, 기준 accession, 분석일시, 프로파일 JSON, 모델명, 프롬프트버전 |
| `peers` | CIK, peer CIK, 출처(SIC/본문추출/수기), 확정여부 |

---

## 10. 포트폴리오로서의 가치

이 프로젝트는 완성되면 그 자체로 실무 역량의 증빙이 된다. 사내 내규 프로그램과 달리 회사 자산이 아니므로 반출 제약 없이 자유롭게 공개할 수 있고, 링크 하나로 결과물을 직접 보여줄 수 있다는 점에서 캡처 몇 장보다 설득력이 크다.

금융권 지원이라는 맥락에서 이 결과물이 말해주는 것은 개발 능력만이 아니다. 어떤 데이터가 어디에 있는지 알고(공시 체계, 거래소 데이터, XBRL), 그것을 어떤 관점으로 엮어야 의미가 생기는지 안다는 증거가 된다. 투자자산운용사 자격과 실제 투자 경험이 있는 사람이 만든 화면과 그렇지 않은 사람이 만든 화면은 지표 선택과 배치에서 갈린다. 소개 페이지에 "왜 이 지표를 이 자리에 뒀는가"를 한두 줄씩 적어두면 그 차이가 드러난다.

다만 미완성 상태로 공개하는 것은 피한다. 기능이 적더라도 끝까지 동작하는 화면이, 기능이 많은데 절반이 깨져 있는 화면보다 낫다. 로드맵의 2단계까지만 완성해서 공개하고 이후 기능을 붙여나가는 편이 안전하다.

---

## 11. 확정 사항

| 항목 | 결정 |
|---|---|
| 증권사 API | 토스증권 Open API |
| 시장 범위 | 국내(KRX) + 미국 |
| 매매 주문·계좌 조회 | 넣지 않음 (읽기 전용 정보 사이트) |
| 인증 | 공용 비밀번호 1개, 이력서에 URL과 함께 기재 |
| 개발 방식 | VS Code + 클로드 코드 |
| 10-K 유니버스 | Tier C(전 종목 XBRL) + Tier A(S&P 500 사전 분석) + Tier B(그 외 온디맨드·영구 캐시) |
| 착수 순서 | EDGAR 모듈과 토스 API 연동을 병행 (EDGAR는 신청 절차 없이 즉시 착수 가능) |

### 아직 열려 있는 것

토스증권의 현재가 API가 복수 종목 동시 조회를 지원하는지 여부. 지원하면 폴링 설계가 훨씬 여유로워지고, 단건만 가능하면 관심종목 수에 따라 주기를 조정해야 한다. API 레퍼런스에서 `symbols` 파라미터 스펙을 확인하는 것이 착수 직후 첫 작업이다.

미국 종목의 시세 갱신 주기. 한국 시간 야간에 폴러를 계속 돌릴지, 접속자가 있을 때만 돌릴지에 따라 서버 부하와 rate limit 소모가 달라진다. 접속자가 없으면 폴링을 멈추는 방식이 합리적이다.

---

## 12. 다음 액션

**이번 주.** 토스증권 계좌를 개설하고 WTS에서 `client_id`·`client_secret`을 발급받는다. 동시에 VPS를 하나 잡아 고정 IP를 확보하고 허용 IP에 등록한다. 이 두 가지가 끝나야 토스 API 호출이 시작되므로 가장 먼저 처리한다. OpenDART 인증키도 같이 신청해 둔다.

**동시에 시작할 수 있는 것.** SEC EDGAR는 신청 절차가 없으므로 오늘 바로 착수 가능하다. 첫 목표는 `company_tickers.json`을 받아 티커→CIK 매핑을 DB에 넣고, 관심 종목 하나의 최신 10-K를 다운로드해 Item 1과 Item 1A를 분리해내는 것까지다. 이게 되면 나머지는 확장이다.

**프로젝트 첫날 해야 할 일.** Git 저장소 생성과 `.gitignore`에 `.env` 등록. 순서를 미루면 반드시 키가 커밋된다.
