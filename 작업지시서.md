# 클로드 코드 작업 지시서

각 단계의 지시문을 **위에서부터 하나씩** 클로드 코드에 붙여넣는다. 한 단계가 실제로 동작하는 것을 확인한 뒤 다음으로 넘어간다. 여러 개를 한꺼번에 붙여넣지 않는다.

각 지시문은 필요한 것을 클로드 코드가 먼저 물어보도록 작성되어 있다. 무언가를 요구하면 알려주고, 그 뒤는 맡기면 된다.

---

## 0단계 — 시작. 무엇을 해야 하는지부터 정리받기

**미리 준비할 것은 없다.** 아래 지시문을 붙여넣으면 클로드 코드가 현재 상태를 확인하고, 해야 할 일을 순서대로 정리해 하나씩 안내한다.

> 새 프로젝트를 시작한다. 이 폴더에 `CLAUDE.md`와 `기획서.md`가 있으니 먼저 둘 다 읽어라.
>
> 나는 개발 경험이 없고, 지금 집에서 처음 시작하는 상태다. 계정도 키도 아무것도 준비되어 있지 않다. 그러니 **내가 해야 할 일을 네가 정리해서 하나씩 알려줘라.**
>
> 순서는 이렇게 해라.
>
> 1. 내 컴퓨터 상태를 확인해라. 운영체제가 무엇인지, Python·Node.js·Git이 설치되어 있는지 버전과 함께 알려줘라. 없는 게 있으면 내 운영체제에 맞는 설치 방법을 알려주고, 내가 설치한 뒤 다시 확인해라.
> 2. 이 프로젝트를 끝까지 만들기 위해 내가 직접 해야 하는 외부 작업(계정 가입, 키 발급, 사이트 설정 등)을 전부 목록으로 정리해라. 각 항목마다 어디서 하는지, 대략 얼마나 걸리는지, 지금 당장 필요한지 나중 단계에서 필요한지 표시해라. 이 목록을 `TODO.md`로 저장하고, 내가 하나씩 끝낼 때마다 체크 표시를 갱신해라.
> 3. 그중 **지금 당장 필요한 것 하나만** 골라서, 어느 사이트에 들어가 어떤 메뉴를 클릭하는지까지 구체적으로 안내해라. 한 번에 하나씩만 시켜라. 내가 끝냈다고 하면 다음 것을 알려줘라. 발급받은 키는 내가 `.env`에 직접 넣겠다.
> 4. 지금 단계에 필요한 준비가 끝나면 프로젝트 폴더 구조를 만들고, Git 저장소를 초기화하고, `.gitignore`(`.env`, `/data`, `__pycache__`, `node_modules`, `.venv` 포함)와 `.env.example`을 만들어라.
> 5. 파이썬 가상환경을 만들고 기본 패키지를 설치한 뒤, `GET /health`가 `{"status":"ok"}`를 반환하는 최소 FastAPI 서버를 띄워서 실제 응답을 보여줘라.
>
> 중간에 내가 뭔가 할 차례가 오면 멈추고 명확히 알려줘라. 알아서 더미 값으로 넘어가지 마라.

### 클로드 코드가 요구할 것들

미리 알고 있으면 마음이 편하다. 순서와 시점은 클로드 코드가 알려주므로 지금 미리 할 필요는 없다.

**토스증권 계좌와 API 키.** 토스 앱에서 비대면 개설(몇 분) → PC 웹(WTS) 로그인 → 설정 > Open API에서 `client_id`와 `client_secret` 발급. 아직 순차 개방 중이라 메뉴가 안 보일 수 있다. 그러면 사전신청만 해두고 4단계(미국 기업 데이터)부터 진행하면 된다. 클로드 코드에게 "토스 Open API 메뉴가 안 보인다"고 말하면 순서를 조정해 줄 것이다.

**허용 IP 등록.** 토스는 등록되지 않은 IP에서의 호출을 403으로 막는다. 집에서 개발하므로 집 IP를 등록하면 된다. 다만 가정용 인터넷은 IP가 바뀔 수 있으니, 잘 되던 것이 갑자기 403이 되면 이것부터 의심한다. 배포용 서버 IP는 8단계에서 등록하면 되고 지금은 필요 없다.

**OpenDART 인증키.** 무료, 즉시 발급.

**Anthropic API 키.** 6단계부터 필요하다. 지금은 없어도 된다.

**서버.** 8단계 배포 때 필요하다. 지금은 없어도 된다. 개발은 전부 집 컴퓨터에서 한다.

---

## 1단계 — 토스증권 인증

> 토스증권 Open API 인증을 붙인다.
>
> 먼저 `https://developers.tossinvest.com/llms.txt`와 `https://openapi.tossinvest.com/openapi-docs/overview.md`를 읽고 인증 방식을 정확히 파악해라. 기억에 의존하지 말고 문서를 확인해라.
>
> 그다음:
>
> 1. 내게 필요한 값을 요청해라. 내가 `.env`에 직접 넣겠다.
> 2. `backend/app/clients/toss.py`에 토스증권 클라이언트를 만든다. 액세스 토큰을 발급받아 파일이나 메모리에 캐싱하고, 만료가 가까우면 자동으로 재발급하고, 401 `expired-token`을 받으면 재발급 후 원래 요청을 한 번 재시도하는 구조로 만들어라.
> 3. API 그룹별 rate limit을 지키는 토큰 버킷을 넣어라. 그룹별 한도는 위 문서에 표로 정리되어 있다.
> 4. 삼성전자(005930) 현재가를 조회해 콘솔에 출력하는 스크립트를 `backend/scripts/`에 만들고 **실제로 실행해서 결과를 보여줘라.**
>
> 403이 뜨면 허용 IP 등록 문제일 가능성이 높으니 내게 확인을 요청해라.
>
> 실행 결과가 나오면, 내가 토스 앱 화면의 값과 대조해 볼 수 있게 종목명·현재가·등락률을 사람이 읽기 좋게 출력해라.

---

## 2단계 — 시세 조회 API와 폴러

> 이제 시세를 서버에서 주기적으로 갱신하는 구조를 만든다.
>
> 먼저 확인할 것이 하나 있다. 토스증권의 현재가 조회 API가 **여러 종목을 한 번에 조회할 수 있는지** OpenAPI JSON(`https://openapi.tossinvest.com/openapi-docs/latest/openapi.json`)에서 `symbols` 파라미터 스펙을 확인하고 내게 알려줘라. 이 결과에 따라 폴링 주기 설계가 달라진다.
>
> 그다음:
>
> 1. 관심종목 목록을 DB에 저장하는 테이블과 CRUD를 만든다. 초기값으로 국내 5종목, 미국 5종목 정도를 넣어라. 어떤 종목을 넣을지는 내게 물어봐라.
> 2. 장 운영 캘린더 API로 KRX와 미국 장 세션 시간을 받아온다. 이걸로 폴러를 자동으로 켜고 끈다. 시간표를 코드에 직접 적지 마라.
> 3. 장중에만 관심종목 시세를 폴링해 메모리 캐시를 갱신하는 백그라운드 폴러를 만든다. 주기는 rate limit에서 역산해 여유 있게 잡아라.
> 4. `GET /api/quotes`로 캐시된 시세를 반환하는 엔드포인트를 만든다. 장 외 시간에는 마지막 종가와 세션 상태("정규장" / "애프터마켓" / "장 마감")를 함께 반환해라.
> 5. 서버를 띄우고 실제 응답을 보여줘라.

---

## 3단계 — 화면 첫 버전

> 프론트엔드를 만든다. React + Vite + TailwindCSS.
>
> 이번 단계 목표는 **관심종목 시세가 실시간으로 갱신되는 화면 하나**다. 그 이상은 만들지 마라.
>
> 1. 백엔드에서 프론트로 시세를 밀어주는 방식을 SSE(Server-Sent Events)로 구현한다.
> 2. 관심종목 그리드를 만든다. 종목명, 현재가, 등락률, 등락폭, 거래량. 상승은 빨강, 하락은 파랑(한국 관행).
> 3. 화면 상단에 현재 세션 상태를 표시해라. 장 마감 시간에 접속해도 화면이 비어 보이지 않아야 한다.
> 4. 개발 서버를 띄우고 브라우저에서 확인할 수 있게 해라. 접속 주소를 알려줘라.
>
> 디자인은 과하게 꾸미지 말고 숫자가 잘 읽히는 것을 우선해라. 폰트는 숫자에 고정폭을 써라.

---

## 4단계 — SEC EDGAR 수집

> 미국 기업 데이터 수집을 시작한다. 이 단계는 토스증권과 무관하므로 독립적으로 진행할 수 있다.
>
> `CLAUDE.md`의 SEC EDGAR 항목에 적힌 접근 규칙을 반드시 지켜라. 특히 User-Agent 헤더와 초당 호출 제한이다. User-Agent에 넣을 이름과 이메일은 내게 물어봐라.
>
> 1. `company_tickers.json`을 받아 티커↔CIK 매핑을 DB에 적재한다.
> 2. 티커를 입력하면 해당 기업의 10-K 목록(제출일, accession number, 원문 URL)을 가져오는 함수를 만든다.
> 3. 특정 10-K 원문을 다운로드해 로컬에 저장하는 함수를 만든다. 한 번 받은 문서는 다시 받지 않도록 캐싱해라.
> 4. AAPL로 테스트해서 최신 10-K가 실제로 저장되는 것을 보여줘라.

---

## 5단계 — 10-K 섹션 분할과 재무 데이터

> 10-K 원문에서 필요한 부분만 뽑아내고, 재무 수치를 확보한다.
>
> 1. 10-K HTML에서 Item 1(Business), Item 1A(Risk Factors), Item 3(Legal Proceedings), Item 7(MD&A)을 분리해 DB에 저장하는 파서를 만든다. 회사마다 문서 구조가 달라 실패할 수 있으니, 실패 시 조용히 넘어가지 말고 로그를 남겨라.
> 2. AAPL, MSFT, KO, XOM 네 종목으로 테스트하고 각 섹션의 앞 200자와 글자 수를 보여줘라. 제대로 잘렸는지 내가 눈으로 확인하겠다.
> 3. `companyfacts` API로 매출·영업이익·순이익·총자산·자본총계·발행주식수의 최근 5개 회계연도 시계열을 가져와 DB에 저장한다. 회사마다 XBRL 태그가 다르므로 대체 태그 후보를 두고 순차 시도하는 방식으로 만들어라.
> 4. 저장된 수치를 표로 출력해 보여줘라.

---

## 6단계 — LLM 서술 분석

> 10-K 섹션을 분석해 기업 프로파일을 만든다. Anthropic API를 쓴다.
>
> API 키를 내게 요청해라. `.env`에 넣겠다.
>
> **가장 중요한 규칙**: 숫자는 LLM에게 시키지 않는다. 매출·이익 등 모든 수치는 5단계에서 확보한 XBRL 데이터를 쓰고, LLM은 서술형 해석만 담당한다.
>
> 1. 섹션별로 요약한 뒤 합치는 구조로 분석 파이프라인을 만든다. 출력은 자유 서술이 아니라 고정된 JSON 스키마로 받아라. 스키마는 기획서 6.4를 참고하되, 초안을 만들어 내게 먼저 보여주고 확인을 받아라.
> 2. 모든 서술 항목에 근거가 된 섹션과 위치를 함께 저장해라. 나중에 내가 원문을 확인할 수 있어야 한다.
> 3. 분석 결과는 DB에 영구 저장한다. 같은 10-K를 두 번 분석하지 않도록 해라.
> 4. 모델은 Haiku로 시작한다. 배치 작업은 Batch API를 쓴다.
> 5. AAPL 하나로 먼저 돌려보고 결과 JSON과 **소요된 토큰 수, 예상 비용**을 보여줘라. 비용을 확인한 뒤 대상을 늘릴지 정하겠다.

---

## 7단계 — 기업 상세 화면

> 티커를 입력하면 기업 리포트가 나오는 화면을 만든다.
>
> 구성은 기획서 6.7을 따른다. 위에서부터 주가 차트와 핵심 지표, 매출·이익 추이 차트, 마진 추이, 리스크와 강점 카드, 공시 타임라인 순이다.
>
> 중요한 것 두 가지다. 첫째, 차트는 전부 XBRL 수치 기반이므로 LLM 분석이 없는 종목도 차트는 나와야 한다. 둘째, 서술 카드에는 근거 섹션으로 가는 링크를 붙여라.
>
> 아직 분석되지 않은 티커를 조회하면 그 자리에서 분석을 시작하고 진행 상태를 보여주는 구조로 만들어라.

---

## 8단계 — 공개 준비

> 이제 외부에서 접속 가능하게 만든다.
>
> 1. 공용 비밀번호 하나로 접근을 제한한다. 비밀번호를 입력하면 세션 쿠키를 발급하는 방식이면 충분하다. 비밀번호는 `.env`에 둬라.
> 2. `robots.txt`로 검색엔진 색인을 차단해라.
> 3. 소개 페이지를 만든다. 프로젝트 개요, 사용 기술, 데이터 소스, 아키텍처 다이어그램, 직접 구현한 부분. 초안을 만들어 보여주면 내가 내용을 다듬겠다.
> 4. 외부 API가 죽어도 캐시된 데이터로 화면이 뜨도록 폴백을 넣어라. 500 에러가 보이면 안 된다.
> 5. 이제 서버가 필요하다. 어떤 서비스를 쓸지 추천하고(무료 옵션이 있으면 알려줘라), 가입부터 서버 생성까지 화면 단위로 안내해라. 서버 IP가 나오면 토스 허용 IP에 등록하는 방법도 알려줘라.
> 6. 배포를 도와줘라. 내가 터미널에 붙여넣을 명령을 하나씩 알려주면 실행하고 결과를 보여주겠다. 한 번에 여러 개 주지 마라.

---

## 그 이후

캘린더, 스크리너, 종목 메모, 알림, 국내 기업의 DART 기반 분석. 여기서부터는 급하지 않으니 하나씩 붙인다. 8단계까지 완성해서 공개한 뒤 확장하는 편이 안전하다.

---

## 막혔을 때

**403이 계속 뜬다** → 토스 허용 IP 등록을 확인한다. EDGAR라면 User-Agent 헤더 문제다.

**클로드 코드가 여러 파일을 고치다 망가뜨렸다** → 마지막 커밋으로 되돌린다. "직전 커밋 상태로 되돌려줘"라고 하면 된다. 그래서 단계마다 커밋한다.

**숫자가 이상하다** → 토스 앱이나 SEC 원문과 대조한다. 단위(원/천원/백만원), 등락률 부호, 환율 환산 시점이 흔한 원인이다.

**비용이 걱정된다** → platform.claude.com 콘솔에서 사용량 알림과 상한을 설정한다. 6단계 시작 전에 해두는 것이 좋다.

**어제까지 되던 게 오늘 403이 뜬다** → 집 IP가 바뀌었을 가능성이 높다. 토스 WTS에서 현재 IP를 다시 등록한다.

**클로드 코드가 한 번에 너무 많이 만들려 한다** → 멈추고 "이번엔 X만 해줘"라고 범위를 좁힌다.
